import assert from 'node:assert/strict';
import fs from 'node:fs';
import path from 'node:path';
import { repoRootFromScript, requestTextNoAuth, startStaticFileServer } from './lib/harness-utils.mjs';

const repoRoot = repoRootFromScript(import.meta.url);
const shellRoot = path.join(repoRoot, 'shell', 'engine-shell', 'web');
const shellPackageJson = fs.readFileSync(path.join(repoRoot, 'shell', 'engine-shell', 'package.json'), 'utf8');
const shellApp = fs.readFileSync(path.join(repoRoot, 'shell', 'engine-shell', 'src', 'App.tsx'), 'utf8');
const shellStyles = fs.readFileSync(path.join(repoRoot, 'shell', 'engine-shell', 'src', 'styles.css'), 'utf8');
const sceneEditorView = fs.readFileSync(path.join(repoRoot, 'shell', 'engine-shell', 'src', 'SceneEditorView.tsx'), 'utf8');
const referenceGuideSource = fs.readFileSync(path.join(repoRoot, 'shell', 'engine-shell', 'src', 'reference-guide.ts'), 'utf8');
const referenceGuideJson = fs.readFileSync(path.join(repoRoot, 'docs', 'reference', 'ENGINE-REFERENCE-GUIDE.json'), 'utf8');
const referenceGuideMarkdown = fs.readFileSync(path.join(repoRoot, 'docs', 'reference', 'ENGINE-REFERENCE-GUIDE.md'), 'utf8');

const server = await startStaticFileServer({
  rootDir: shellRoot,
  host: '127.0.0.1',
  port: 0,
});

try {
  const [html, styleCss, codeJs] = await Promise.all([
    requestTextNoAuth(`${server.baseUrl}/`),
    requestTextNoAuth(`${server.baseUrl}/css/style.css`),
    requestTextNoAuth(`${server.baseUrl}/js/pages/code.js`),
  ]);

  assert.match(html, /<script type="module" src="\.\/js\/code-bridge\.js"><\/script>/);
  assert.match(styleCss, /\.code-editor-search-match\s*\{/);
  assert.match(styleCss, /\.code-editor__toolbar-search\s*\{/);
  assert.match(codeJs, /data-code-editor-search-input/);
  assert.match(codeJs, /data-code-editor-search-prev/);
  assert.match(codeJs, /data-code-editor-search-next/);
  assert.match(codeJs, /findMatches\(/);
  assert.match(codeJs, /setEditorSearchQuery\(/);
  assert.match(shellPackageJson, /"react"/);
  assert.match(shellPackageJson, /"vite"/);
  assert.match(shellApp, /lazy\(\(\) => import\('\.\/SpatialAttachmentEditorView'\)/);
  assert.match(shellApp, /import\('@xterm\/xterm'\)/);
  assert.doesNotMatch(shellApp, /^import .*from '@xterm\//m);
  assert.match(shellApp, /const centerTabs = SHELL_WORKSPACES;/);
  assert.doesNotMatch(shellApp, /const centerTabs = \['Scene', 'Game', 'Preview', 'Code', 'Guide'\] as const;/);
  assert.match(shellApp, /useState<ShellLayout>\(\(\) => loadBrowserShellLayout\(\)\)/);
  assert.match(shellApp, /saveShellLayout\(getBrowserShellLayoutStorage\(\), next\)/);
  assert.match(shellApp, /resetShellLayout\(getBrowserShellLayoutStorage\(\)\)/);
  assert.match(shellApp, /aria-label="Layout controls"/);
  assert.match(shellApp, /title="Toggle left sidebar"/);
  assert.match(shellApp, /title="Toggle bottom dock"/);
  assert.match(shellApp, /title="Toggle right sidebar"/);
  assert.match(shellApp, /title="Reset all workspace layouts"/);
  assert.match(shellApp, /aria-label="Resize left sidebar"/);
  assert.match(shellApp, /aria-label="Resize right sidebar"/);
  assert.match(shellApp, /aria-label="Resize bottom dock"/);
  assert.doesNotMatch(shellApp, /const \[activeCenterTab, setActiveCenterTab\]/);
  assert.match(shellStyles, /\.shell-grid--both\s*\{/);
  assert.match(shellStyles, /\.shell-pane-resize-handle:focus-visible::after/);
  assert.match(shellStyles, /\.scene-editor__body\s*\{[\s\S]*?overflow-y:\s*auto;/);
  assert.match(shellStyles, /\.scene-editor__sidebar\s*\{[\s\S]*?min-height:\s*360px;/);
  assert.match(shellApp, /web\/index\.html#\/code/);
  assert.match(shellApp, /CodeWorkspaceView/);
  assert.match(shellApp, /Load legacy bridge/);
  assert.doesNotMatch(shellApp, /bridge-placeholder/);
  assert.match(shellApp, /ReferenceGuideView/);
  assert.match(shellApp, /aria-label="Primary workspaces"/);
  assert.match(shellApp, /Play, stop, and restart the game in its own window/);
  assert.match(shellApp, /Arrange and tune the world, then press Play to test it/);
  assert.match(shellApp, /scene-editor-host/);
  assert.match(shellApp, /hidden=\{showGuide \|\| activeCenterTab !== 'World'\}/);
  assert.match(shellApp, /The game opens in a separate window/);
  assert.match(shellApp, /<summary>Diagnostics<\/summary>/);
  assert.match(shellApp, /Run existing build/);
  assert.doesNotMatch(shellApp, /toolbar-cluster toolbar-cluster--center/);
  assert.doesNotMatch(shellApp, /fetchAiProviders/);
  assert.doesNotMatch(shellApp, /runAiSmokeTest/);
  assert.doesNotMatch(shellApp, /<h3>AI<\/h3>/);
  assert.match(shellApp, /engine_sessiond/);
  assert.match(shellApp, /if \(createdTerminal\) \{\s*instance\?\.terminal\.focus\(\);/s);
  assert.match(shellApp, /aria-label="Terminals"[\s\S]*role="tablist"/);
  assert.match(shellApp, /className="terminal-tab__select"/);
  assert.match(shellApp, /aria-label="Terminal shell"/);
  assert.match(shellApp, /id="terminal-viewport-panel"/);
  assert.match(shellApp, /!bottomPaneCollapsed \? \([\s\S]*?Maximize/);
  assert.doesNotMatch(shellApp, /<button[^>]*className=\{`terminal-tab[\s\S]*?<button[^>]*terminal-tab__close/);
  assert.match(shellApp, /Edit Workspace/);
  assert.match(shellApp, /Workspaces/);
  assert.match(shellApp, /Explorer/);
  assert.match(shellApp, /Source Control/);
  assert.match(shellApp, /Workspace root/);
  assert.match(shellApp, /Init repo/);
  assert.match(shellApp, /fetchGitStatus/);
  assert.match(shellApp, /initGitRepository/);
  assert.match(shellApp, /updateSession/);
  assert.match(shellApp, /deleteSession/);
  assert.match(shellApp, /listHostDirectories/);
  assert.match(shellApp, /dir-picker/);
  assert.match(shellApp, /fetchPlatformInfo/);
  assert.match(shellApp, /readFile/);
  assert.match(shellApp, /listFiles/);
  assert.match(shellApp, /startRuntimeBuild/);
  assert.match(shellApp, /fetchBuildStatus/);
  assert.match(shellApp, /SceneEditorView/);
  assert.match(sceneEditorView, /handleModeChange\('play'\)/);
  assert.match(sceneEditorView, /Verify/);
  assert.match(sceneEditorView, /Verify locks editing and keeps your changes/);
  assert.match(sceneEditorView, /Save world/);
  assert.match(sceneEditorView, /Save object/);
  assert.match(sceneEditorView, /Duplicate world/);
  assert.match(sceneEditorView, /Browser preview is not connected yet/);
  assert.match(sceneEditorView, /Play opens the game in a separate window/);
  assert.match(sceneEditorView, /id="scene-sidebar-panel-outliner"/);
  assert.match(sceneEditorView, /Run existing build/);
  assert.match(sceneEditorView, /async function handlePlay/);
  assert.match(sceneEditorView, /aria-pressed=\{mode === 'edit'\}/);
  assert.match(sceneEditorView, /aria-pressed=\{mode === 'play'\}/);
  assert.match(sceneEditorView, /onClick=\{\(\) => void handlePlay\(\)\}/);
  assert.match(shellApp, /Play needs CMake/);
  assert.match(shellApp, /Run existing build in Diagnostics/);
  assert.match(shellApp, /auto-detect common installs and export SHADER_FORGE_CMAKE/);
  assert.match(shellApp, /Visual Studio CMake is working, but SDL3 development files and the Vulkan SDK\/loader were not found/);
  assert.match(shellApp, /playtestFailureMessage/);
  assert.match(shellApp, /Play needs attention/);
  assert.match(shellApp, /sdl3\[vulkan\]:x64-windows/);
  assert.match(shellApp, /--recurse/);
  assert.match(sceneEditorView, /Build \+ Run needs CMake/);
  assert.match(sceneEditorView, /auto-detect common installs and export/);
  assert.match(sceneEditorView, /SHADER_FORGE_CMAKE/);
  assert.match(sceneEditorView, /Native runtime dependencies missing/);
  assert.match(sceneEditorView, /Use for world/);
  assert.match(sceneEditorView, /Add object/);
  assert.match(sceneEditorView, /Duplicate/);
  assert.match(sceneEditorView, /Delete/);
  assert.match(sceneEditorView, /Add to world/);
  assert.match(sceneEditorView, /Position/);
  assert.match(sceneEditorView, /Rotation/);
  assert.match(sceneEditorView, /Scale/);
  assert.match(sceneEditorView, /Advanced settings/);
  assert.match(sceneEditorView, /Geometry asset/);
  assert.match(sceneEditorView, /Effect event/);
  assert.match(sceneEditorView, /Object ID/);
  assert.match(referenceGuideSource, /ENGINE-REFERENCE-GUIDE\.json/);
  assert.match(referenceGuideJson, /"Shader Forge Reference Guide"/);
  assert.match(referenceGuideJson, /"assistantEntryPoints"/);
  assert.match(referenceGuideJson, /"Scene, Prefab, And Data Foundation"/);
  assert.match(referenceGuideJson, /"engine bake"/);
  assert.match(referenceGuideJson, /engine migrate detect/);
  assert.match(referenceGuideJson, /audio\/buses\.toml/);
  assert.match(referenceGuideJson, /animation\/skeletons\/\*\.skeleton\.toml/);
  assert.match(referenceGuideJson, /physics\/layers\.toml/);
  assert.match(referenceGuideJson, /content\/procgeo\/\*\.procgeo\.toml/);
  assert.match(referenceGuideMarkdown, /# Shader Forge Reference Guide/);
  assert.match(referenceGuideMarkdown, /docs\/reference\/ENGINE-REFERENCE-GUIDE\.json/);
  assert.match(referenceGuideMarkdown, /engine bake/);
  assert.match(referenceGuideMarkdown, /engine migrate detect/);
  assert.match(referenceGuideMarkdown, /audio\/buses\.toml/);
  assert.match(referenceGuideMarkdown, /animation\/skeletons\/\*\.skeleton\.toml/);
  assert.match(referenceGuideMarkdown, /physics\/layers\.toml/);
  assert.match(referenceGuideMarkdown, /content\/procgeo\/\*\.procgeo\.toml/);
  assert.match(referenceGuideMarkdown, /## Runtime And Authoring/);
  assert.match(referenceGuideMarkdown, /## Input, Tooling, And Testing/);

  console.log('Engine shell smoke passed.');
  console.log(`- Served shell from ${server.rootDir}`);
  console.log('- Verified index, style sheet, and preserved code workspace module');
  console.log('- Verified inline editor search UI and Monaco match-finding hooks are present');
  console.log('- Verified the React/Vite shell frame, optional renderer splitting, terminal tab semantics, and World mode states');
  console.log('- Verified the React shell exposes an in-app guide backed by repo-native markdown and structured reference sources');
  console.log('- Verified the React shell references the session backend bridge contract');
  console.log('- Verified the Explorer tab references backend file list/read flows');
  console.log('- Verified the shell references runtime build and runtime lifecycle controls');
} finally {
  await server.close();
}
