{
  "title": "Shader Forge Reference Guide",
  "intro": "Searchable operator and assistant wiki for the current Shader Forge shell, session backend, native runtime, tooling, content data, and deterministic verification workflow.",
  "searchableSources": [
    "docs/reference/ENGINE-REFERENCE-GUIDE.md",
    "docs/reference/ENGINE-REFERENCE-GUIDE.json",
    "plans/ENGINE-IMPLEMENTATION-PLAN.md",
    "docs/specs/ENGINE-SYSTEMS-INDEX.md",
    "docs/specs/ENGINE-MCP-SPEC.md",
    "docs/guides/SHADER-FORGE-MCP-SETUP.md",
    "docs/specs/ENGINE-OPERATIONS-SPEC.md",
    "docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md",
    "AGENTS.md"
  ],
  "assistantEntryPoints": [
    "docs/reference/ENGINE-REFERENCE-GUIDE.md",
    "docs/reference/ENGINE-REFERENCE-GUIDE.json",
    "AGENTS.md",
    "plans/ENGINE-IMPLEMENTATION-PLAN.md",
    "docs/specs/ENGINE-SYSTEMS-INDEX.md",
    "docs/specs/ENGINE-MCP-SPEC.md",
    "docs/guides/SHADER-FORGE-MCP-SETUP.md",
    "docs/specs/ENGINE-OPERATIONS-SPEC.md",
    "docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md"
  ],
  "categories": [
    {
      "id": "getting-started",
      "title": "Getting Started",
      "description": "Find the main surfaces quickly, understand where the live reference lives, and know which current docs should anchor day-to-day work.",
      "keywords": ["guide", "wiki", "assistant", "operator", "search", "reference"],
      "pages": [
        {
          "id": "reference-sources",
          "title": "Reference Sources",
          "summary": "Use the repo-native guide files first so external development agents and shell users resolve the same current guidance.",
          "keywords": ["docs", "json", "markdown", "searchable", "assistant-facing"],
          "references": [
            "docs/reference/ENGINE-REFERENCE-GUIDE.md",
            "docs/reference/ENGINE-REFERENCE-GUIDE.json",
            "plans/ENGINE-IMPLEMENTATION-PLAN.md",
            "docs/specs/ENGINE-SYSTEMS-INDEX.md",
            "docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md",
            "AGENTS.md"
          ],
          "sections": [
            {
              "title": "Primary Sources",
              "keywords": ["entry points", "lookup", "index"],
              "items": [
                "Use `docs/reference/ENGINE-REFERENCE-GUIDE.md` as the plain-text guide for terminal assistants and repo search.",
                "Use `docs/reference/ENGINE-REFERENCE-GUIDE.json` as the structured guide source for the shell and external development agents.",
                "Use `plans/ENGINE-IMPLEMENTATION-PLAN.md` to check current phase order, progress, and dependency gates.",
                "Use `docs/specs/ENGINE-SYSTEMS-INDEX.md` to jump to the subsystem specs that define the current architecture.",
                "Use `docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md` for the implemented native spatial schema/query/cook/rest evaluation, v2 sampled two-bone secondary-hand IK, operation-scoped validation, exact Assets rest/sampled rig workbench, and immutable rendered review workflow.",
                "Use `AGENTS.md` for repo workflow rules, documentation obligations, and required update discipline."
              ]
            },
            {
              "title": "Assistant Lookup Workflow",
              "keywords": ["workflow", "assistant", "terminal", "native"],
              "items": [
                "Start with the markdown guide when you need a quick terminal-readable overview of current behavior.",
                "Use the JSON guide when an assistant needs structured categories, page ids, references, or search terms.",
                "Use the implementation plan and subsystem specs to widen from current behavior into target architecture."
              ]
            },
            {
              "title": "Update Discipline",
              "keywords": ["sync", "maintenance", "documentation"],
              "items": [
                "When user-facing behavior, shell workflow, runtime control, or assistant-facing engine behavior changes, update the guide in the same pass.",
                "Treat the guide as operator and assistant working documentation, not marketing copy or a post-hoc changelog.",
                "Keep the guide concrete enough that a coding assistant in a terminal can search it and act on it without needing the whole codebase first."
              ]
            }
          ]
        },
        {
          "id": "native-runtime-setup",
          "title": "Native Runtime Setup",
          "summary": "The clean-start scripts now auto-detect CMake, but the real Windows native-runtime path also needs SDL3 development files and the Vulkan SDK.",
          "keywords": ["install", "setup", "cmake", "vulkan", "sdl3", "vcpkg", "windows"],
          "references": [
            "scripts/start-dev-clean.ps1",
            "scripts/start-dev-clean.sh",
            "tools/shared/cmake-command.mjs",
            "engine/runtime/CMakeLists.txt",
            "docs/specs/ENGINE-SHELL-SPEC.md",
            "docs/specs/ENGINE-CLI-SPEC.md"
          ],
          "sections": [
            {
              "title": "Current Setup Rules",
              "keywords": ["startup", "autodetect", "windows", "visual studio"],
              "items": [
                "The guide now has an explicit native-runtime setup section for Windows.",
                "`.\\scripts\\start-dev-clean.ps1` now auto-detects CMake, including the copy bundled with Visual Studio, and exports `SHADER_FORGE_CMAKE` when found.",
                "`.\\scripts\\install-windows-native-runtime-deps.ps1` is the repo helper for the Windows native-runtime dependency lane.",
                "If the startup script prints `Using CMake: ...`, CMake itself is not the blocker.",
                "The real native runtime still requires SDL3 development files plus the Vulkan SDK/loader."
              ]
            },
            {
              "title": "Recommended Windows Install Path",
              "keywords": ["vcpkg", "vulkan sdk", "toolchain file"],
              "items": [
                "On Windows, install the Vulkan SDK from LunarG.",
                "For the current Shader Forge setup, the Vulkan installer should use `The Vulkan SDK Core` only; the extra optional SDK components are not required for this repo right now.",
                "On Windows, the recommended SDL3 path is `vcpkg`, not Visual Studio Installer.",
                "Recommended one-step repo helper: `powershell.exe -ExecutionPolicy Bypass -File .\\scripts\\install-windows-native-runtime-deps.ps1`.",
                "That helper clones `vcpkg` if needed, bootstraps it, installs or rebuilds `sdl3[vulkan]:x64-windows` with `--recurse`, sets `VCPKG_ROOT` and `CMAKE_TOOLCHAIN_FILE` for the current process, and persists them to the user environment by default.",
                "If `Play` reaches SDL startup and then says Vulkan support is not configured in SDL, the installed SDL3 package is missing the `vulkan` feature; rerun `.\\scripts\\install-windows-native-runtime-deps.ps1` or run `C:\\src\\vcpkg\\vcpkg.exe install sdl3[vulkan]:x64-windows --recurse`.",
                "Recommended environment variables for CMake-based Windows runs: `VCPKG_ROOT=C:\\src\\vcpkg` and `CMAKE_TOOLCHAIN_FILE=%VCPKG_ROOT%\\scripts\\buildsystems\\vcpkg.cmake`."
              ]
            },
            {
              "title": "How To Recognize The Blocker",
              "keywords": ["stub mode", "diagnostics", "build logs"],
              "items": [
                "`.\\scripts\\start-dev-clean.ps1` now also auto-detects the `vcpkg` toolchain file and the Vulkan SDK when they are installed.",
                "After installing Vulkan SDK or SDL3 through `vcpkg`, reopen PowerShell and rerun `.\\scripts\\start-dev-clean.ps1`.",
                "If build logs say `SDL3 was not found`, `Vulkan was not found`, or `built in stub mode`, that means CMake worked but the native runtime dependencies are still missing."
              ]
            }
          ]
        },
        {
          "id": "shell-workspace",
          "title": "Shell Workspace Overview",
          "summary": "The React shell is the current operator workspace for sessions, bounded native code editing, source control, runtime control, release packaging, diagnostics capture, world authoring, and assets, with strict per-workspace layout persistence and responsive pane controls.",
          "keywords": ["shell", "workspace", "tabs", "rails", "layout", "persistence", "drawer", "world", "playtest", "assets", "package", "profile"],
          "references": [
            "shell/engine-shell/src/App.tsx",
            "shell/engine-shell/src/CodeWorkspaceView.tsx",
            "shell/engine-shell/src/MonacoCodeEditor.tsx",
            "shell/engine-shell/src/SceneEditorView.tsx",
            "shell/engine-shell/src/scene-authoring.ts",
            "shell/engine-shell/src/ReferenceGuideView.tsx",
            "shell/engine-shell/src/shell-layout.ts",
            "shell/engine-shell/src/styles.css",
            "docs/specs/ENGINE-SHELL-SPEC.md"
          ],
          "sections": [
            {
              "title": "Primary Shell Surfaces",
              "keywords": ["left rail", "center dock", "right panel", "bottom dock"],
              "items": [
                "The left rail currently exposes `Workspaces`, `Explorer`, and `Source Control`.",
                "The primary workspace navigation exposes `World`, `Code`, `Playtest`, and `Assets`.",
                "The right panel currently exposes `Runtime`, `Build`, and `Workspace` in `Code` and `Playtest`, while `World` and `Assets` use the center area directly.",
                "The bottom dock currently exposes `Terminal`, `Logs`, `Output`, and `Activity`.",
                "One strict v1 browser record persists the active workspace, each workspace's left/right visibility, selected tabs and preferred widths, and the shared bottom-dock tab, collapsed state, and preferred height. It stores layout chrome only, never sessions, paths, credentials, drafts, operations, or terminal state.",
                "Use the header `Left`, `Right`, and `Bottom` toggles plus `Reset` to manage layout. Visible panes support bounded pointer and keyboard resizing. At 800 pixels or narrower the right pane becomes an on-demand overlay drawer and its toggle remains reachable.",
                "`Playtest`, `World`, and `Assets` start with both side panes hidden; `Code` starts with the left pane visible; the bottom dock starts collapsed. Malformed or unavailable browser storage falls back to those defaults.",
                "The bottom dock can now be resized vertically from its top edge and explicitly `Collapse`d, `Restore`d, or `Maximize`d so terminal/log surfaces do not overlap the main workspace.",
                "The shell loads the optional Assets implementation and xterm renderer only when those surfaces open; World and Code remain mounted so their drafts survive ordinary navigation.",
                "The `Workspace` right-panel tab now also exposes export-preset inspection, release-layout readiness, package generation with the default sibling ZIP archive, visible prep state, and persistent last-package/archive readiness for the selected workspace.",
                "The `Workspace` right-panel tab now also exposes a live diagnostics snapshot plus capture-report controls for the selected workspace, including runtime/build state, packaging readiness, stored capture history, and first profiling recommendations.",
                "The `Workspace` right-panel tab now also exposes the active code-trust policy summary, supported authored hot-reload roots, tracked artifact hashes and verification state, explicit promote/quarantine controls, and pending code-trust approvals for the selected workspace plus the shared engine lane."
              ]
            },
            {
              "title": "Current Workflow Shape",
              "keywords": ["monaco", "preview", "scene", "guide"],
              "items": [
                "Use `Code` for bounded workspace-file browsing, revision-bound Monaco tabs, inline search beside `Inspect`, source/draft diffing, and explicit Preview/Approve/Reject/Apply/Undo. Unsaved drafts survive shell navigation; changing the active project session keeps foreign dirty tabs read-only and detached until their original session returns. A changed draft stales its prior preview. Request/session/tab/path/operation guards reject late reads, events, and actions; apply/undo refresh the baseline only if the draft still equals the captured pre-action draft. Typed state, revision, and code-trust conflicts preserve the draft and adopt returned operation state only when id, session, and path match.",
                "The preserved `web/` editor remains available only through `Load legacy bridge` or `Open standalone`; it is not the default Code workflow.",
                "Use `Playtest` for a compact human loop around the external native runtime window. It names the current world, states whether the game is stopped, building, running, or paused, and shows only state-valid `Play`, `Stop`, `Restart`, `Pause`, or `Resume` actions by default. Build controls, paths, bridge facts, and recent logs stay under collapsed `Diagnostics`, while build and runtime failures remain visible beside the primary surface.",
                "Use `World` for the plain-language view, verify, play, and tune loop. Its sidebar tabs are `World`, `Objects`, `Selection`, and `Library`; `Edit` enables changes and `Verify` is a read-only view of the same in-memory drafts. `Play` saves dirty world and object drafts before building and launching; while changed content is already running, `Apply and restart` saves and relaunches it. Scene/prefab saves, creates, and duplicates use revision-bound semantic operations with exact write leases and native validation. World stays mounted during shell navigation, and an incompatible project/session change prompts before discard or leaves dirty drafts visibly detached until their original workspace returns. World load, reload, save, create, and duplicate responses retain request generation plus workspace/path authority, so returning to an earlier workspace cannot revive a stale response. Runtime/build request generations discard late HTTP completions after newer commands or SSE events, stopped builds cancel pending auto-run, and runtime telemetry does not replace the selected authored world.",
                "Explorer, Git, package, profiling, trust-summary, and trust-approval refreshes are bound to both their request generation and the active workspace. Package, capture, trust, and Activity review actions also retain the workspace-selection generation. Switching workspaces invalidates these immediately, clears their transient busy state and pending build auto-run intent, and ignores late data, errors, or action messages.",
                "Workspace-list and host-directory-picker reads are also generation-bound. Newer loads win, closing the picker cancels its pending navigation result, and workspace selection/edit/delete stay disabled while create/update/delete/refresh is active.",
                "Terminal tabs expose separate, keyboard-navigable tab and close controls, name the shell selector and terminal panel, and announce connection state. World Edit/Verify expose pressed state, workspace edit/delete buttons name their target, and a collapsed bottom dock presents only Restore.",
                "Use `Assets` for the constrained attachment-profile tuner and native-evaluated rest/sampled rig schematics. Browsing and authored sample evaluation are lease-free; editing starts only after `Begin tuning` acquires the exact attachment lease.",
                "If `Play` cannot build because `cmake` is missing, the shell surfaces setup guidance; the clean-start scripts also auto-detect common CMake installs and export `SHADER_FORGE_CMAKE` when possible. The low-level `Run existing build` action under `Diagnostics` still depends on a runtime binary under `build/runtime/bin`.",
                "A successful native build can still be a stub runtime if SDL3 or Vulkan are missing; the shell now surfaces that separately as native dependency setup rather than another CMake problem.",
                "Use `Help` to open the Guide as a secondary searchable reference surface.",
                "External development agents connect through Shader Forge MCP (`sf-mcp`); the shell does not contain provider selection, prompts, or an assistant chat surface."
              ],
              "note": "The shell is still the primary workspace even though the native SDL3/Vulkan runtime renders in an external window."
            }
          ]
        }
      ]
    },
    {
      "id": "session-backend-and-cli",
      "title": "Session Backend And CLI",
      "description": "The shell, `engine` CLI, and Shader Forge MCP (`sf-mcp`) share the `engine_sessiond` control plane for project state and multi-agent coordination.",
      "keywords": ["sessiond", "cli", "backend", "git", "pty", "files", "ai", "mcp", "sf-mcp", "coordination", "operations"],
      "pages": [
        {
          "id": "sessiond-and-files",
          "title": "Workspaces, Files, And Source Control",
          "summary": "Session creation, persistence, safe text/binary reads, revision-safe operations, durable bounded spatial-validation journal data, transient spatial evaluation, lease-gated spatial recapture, immutable review packets/captures, host-root picking, git status, AI inspection, release packaging, and diagnostics capture are implemented in engine_sessiond.",
          "keywords": ["sessions", "explorer", "source control", "workspace root", "ai", "package", "profile", "operations", "revision", "spatial evaluation", "validation journal"],
          "references": [
            "tools/engine-sessiond/server.mjs",
            "tools/engine-sessiond/lib/spatial-attachment-service.mjs",
            "tools/engine-sessiond/lib/runtime-store.mjs",
            "tools/engine-sessiond/lib/operation-store.mjs",
            "shell/engine-shell/src/lib/sessiond.ts",
            "docs/specs/ENGINE-SESSIOND-SPEC.md",
            "docs/specs/ENGINE-OPERATIONS-SPEC.md"
          ],
          "sections": [
            {
              "title": "Implemented Backend Surfaces",
              "keywords": ["http", "api", "pty", "terminal"],
              "items": [
                "`engine_sessiond` currently provides session create/list/get/update/delete. Session `rootPath` is immutable after creation; changing workspace identity requires delete/recreate.",
                "Session records now persist across `engine_sessiond` restarts and stay available until deleted.",
                "Safe file list, file read, and file write APIs are now available inside the active session root.",
                "`engine_sessiond` now also owns a revision-safe text-file write operation workflow: preview, list/get, approve/reject, apply, undo, durable journal recovery, and restart-safe history.",
                "World scene/prefab save, create, and duplicate use `POST /api/operations/scene-asset/preview`, canonical `scene/world/<id>` or `scene/prefab/<id>` leases, exact target/source revisions, native `DataFoundation` validation, and an authoritative post-apply reread. Queued, conflicting, late, or uncertain results preserve the draft and are never blindly retried.",
                "File-write operations persist in the sessiond state directory so Activity/Changes history survives backend restart. Credentials are never stored. Invalid persisted records are skipped on load after preview-schema and event-sequence validation.",
                "Previewed or approved spatial-attachment operations may persist one latest strict validation summary plus at most eight actor-attributed `validated` events. Each summary is bound to the proposed revision and contains at most 64 phase/time samples whose joint-limit, overlap, and tolerance totals must exactly match its findings.",
                "Validation recording compare-and-swaps both proposed revision and `updatedAt`, preserves monotonic operation/event timestamps even under a frozen host clock, and rechecks canonical workspace identity. Reload rejects malformed summaries, invalid spatial context or revision binding, missing or actorless validation events, over-limit history, and backward or out-of-envelope timestamps.",
                "`POST /api/operations/:id/validate` now produces that summary for previewed/approved spatial operations without a lease. It verifies private proposed bytes in a complete staged animation/content/foundation snapshot, native-validates, evaluates rest plus at most 64 exact non-duplicate phase/time samples, rejects source/CAS drift, and exposes neither proposed content nor raw native diagnostics.",
                "Each operation stores the canonical workspace-root identity captured at preview. Apply, undo, and recovery reject a mismatched live session root.",
                "Revisions are SHA-256 content hashes plus an explicit `missing` sentinel. Stale preview/apply/undo calls return HTTP 409 with a structured conflict.",
                "Direct `/api/files/write`, operation apply/undo, and code-trust promote/quarantine share one serialized `SessionStore` file-mutation queue. Compare-and-write/remove run optional `beforeMutation` after revision/identity inspection and before source mutation. Apply/undo persist `applying`/`undoing` before touching the project file and recover by revision comparison after a crash or persistence failure.",
                "Failure and recovery append `apply_failed`, `undo_failed`, or `recovered` events and never replace persisted transitions. Recovery is attributed to the last applying/undoing actor, not the proposer.",
                "Replacement uses a same-directory temp-file + rename without deleting the destination first. Existing POSIX mode bits are preserved where the host supports them. Non-UTF-8 existing files are rejected.",
                "Approve/reject/apply/undo require an explicit valid actor and never default to anonymous human. Recorded actors are local provenance, not cryptographic attribution.",
                "Operation apply reuses the existing code-trust evaluate / review-queue path used by `POST /api/files/write`, but artifact recording is a journaled idempotent effect that must succeed before the operation is `applied`. Apply snapshots prior artifacts inside the mutation lane before source bytes change. Undo provenance precheck and artifact restore run in that same lane so a promote/quarantine cannot interleave. Undo refreshes, reverts, or tombstones that artifact so trust metadata does not claim reverted bytes are still applied.",
                "All supported mutations, including CLI `engine policy promote|quarantine`, go through `engine_sessiond`. Artifact files use serialized atomic replacement. Workspace identity is canonical path plus filesystem `dev`/`ino`. Cooperative engine clients are covered; hostile out-of-process filesystem swaps at the OS syscall boundary are not an adversarial security guarantee.",
                "Persisted operations validate coherent applying/apply and undoing/undo effect shapes. A fabricated `applying`+`recorded` record without evaluation/artifact is skipped. `applying`+`recorded` and `undoing`+`reverted` crash windows finalize without repeating the effect. Legacy session `rootIdentity` is persisted during load.",
                "`engine_sessiond` binds only loopback hosts. Non-loopback bind hosts including `0.0.0.0` and `::` are rejected until an authenticated remote mode exists.",
                "Public JSON requests are rejected with HTTP 413 as soon as accumulated input exceeds 1 MiB, including chunked bodies without `Content-Length`; health advertises the semantic scene-asset capability.",
                "Non-loopback browser Origins are rejected at the sessiond HTTP boundary. No-Origin native CLI/MCP requests and loopback shell Origins remain allowed. This is a local trust boundary, not client authentication.",
                "Operation lifecycle events stream on `/api/events`. `sf-mcp` spatial and whole scene/prefab mutation tools use process-owned coordinator credentials and leases through this same contract; generic MCP mutation remains disabled.",
                "`GET /api/spatial/attachment/evaluate` reads an existing attachment at an exact SHA-256 revision, rejects symbolic paths, stages exact animation files plus every authored content TOML and the data-foundation manifest, validates the source-to-profile mapping, and rechecks the complete sorted revision manifest before returning. `GET /api/spatial/attachment/evaluate-sample` adds an exact authored phase/time under the same binding. Neither route requires a lease or persists evidence.",
                "Spatial preview validates and evaluates exact staged baseline/candidate bytes, binds evaluator output to the validator-selected ID/schema/skeleton/item/mode/perspective contract, and rechecks its write lease after evaluation immediately before operation creation. A new-file baseline is null. Evaluation reports are immediate response data only and remain `pose.sampled=false`, not review evidence.",
                "Assets binds authored evaluations to active session/path/revision and candidate evaluations to operation/base/proposed/state. Draft edits mark cached geometry stale instead of moving it. The complete report is bounded and deep-validated before rendering; malformed nested data or unsafe projection bounds make the schematic unavailable.",
                "`App` retains one sessiond SSE subscription. Assets receives an active-session operation epoch and authoritatively refetches its current operation with session/selection/operation guards. Approval updates actions. Conflict rereads authored bytes, keeps the candidate visibly stale, and retains its captured connection only after successful parse and refreshed resource coverage. Reject/apply/undo clear and reread evidence while releasing/disconnecting only the connection captured by that event/action, never a newer one. Undo after apply requests a fresh lease.",
                "The shell `Activity` bottom-dock tab lists durable operations for the active workspace, reads selected public detail and a selected-only bounded structured text diff, refreshes from operation SSE events, and supports fixed-shell-actor Approve/Reject. It has no Apply, Undo, lease, registration, or credential path.",
                "Activity binds each diff to the selected operation id, path, and before/after revisions, then shows exact old/new line coordinates, line endings, three-line context, truncation, or a truthful binary/too-large/unavailable summary-only reason. Raw journal content bytes are not published. Public validation summaries now exist, but Activity does not render them yet.",
                "See `docs/specs/ENGINE-OPERATIONS-SPEC.md` for the canonical contract.",
                "Runtime start and restart can now launch against the selected session root so shell authoring and runtime testing point at the same project files.",
                "Runtime start and restart now also derive a save root under `<session-root>/saved/runtime` so quick-saves persist with the active project instead of the backend process directory.",
                "`GET /api/ai/providers` and `POST /api/ai/test` expose deterministic fake, optional Ollama, and enabled OpenRouter provider inspection/smoke tests from `engine_sessiond`, without returning provider credentials. Manifest request policy permits zero through two retries and up to two explicit fallback providers; only transient network and selected retryable HTTP failures enter the bounded retry/fallback path. Collection `POST`/`GET /api/ai/jobs` plus per-job `GET`/`DELETE` provide a bounded process-scoped one-active-request queue with result-free listings, status, cancellation, provider abort propagation, and metadata-only `ai.job` lifecycle events on the existing SSE stream. Terminal jobs atomically enter a bounded 128-record metadata-only workspace history; `GET /api/ai/history` and `engine ai history` expose status/limit-filtered reads without prompts, responses, raw errors, or credentials. Successful queued calls with provider-reported usage atomically persist cumulative per-workspace/provider token counts; `GET /api/ai/usage` and `engine ai usage` expose the credential-free summary. An opt-in `monthly_request_limit` durably caps queued non-fake admissions before provider execution; `GET /api/ai/budget` and `engine ai budgets` report it without claiming token pricing or dollar spend. The bounded read-only `ai/registry.json` declares tool schemas, clients, and composed skills; exact schema-validated invoke/run API and CLI paths map only to built-in handlers, including the existing `profile:live` runtime diagnostics service, and grant no mutation authority.",
                "`GET /api/package/inspect` and `POST /api/package/run` expose the Phase 6.2 release-layout flow from `engine_sessiond`, including package prep state, optional auto-bake execution, and the default sibling ZIP archive.",
                "`GET /api/profile/live`, `GET /api/profile/captures`, and `POST /api/profile/capture` now expose the first Phase 6.3 diagnostics snapshot, capture-history, and capture-report lanes from `engine_sessiond`.",
                "`GET /api/code-trust/summary` and `POST /api/code-trust/evaluate` now expose the shared code-trust boundary for shell, CLI, and future assistant clients.",
                "`GET /api/code-trust/artifacts` and `POST /api/code-trust/artifacts/transition` now expose tracked artifact hashes, verification state, and explicit promote/quarantine transitions through the SessionStore mutation lane.",
                "`GET /api/code-trust/approvals` and `POST /api/code-trust/approvals/:id/decision` now expose the review queue for `review_required` code-trust operations.",
                "Policy-relevant file writes now record origin and trust-tier metadata under `<session-root>/.shader-forge/code-trust-artifacts.json`.",
                "Runtime build and runtime start/restart now pass through the same code-trust policy layer before compile or load transitions continue.",
                "Host filesystem directory listing is already used by the workspace-root picker.",
                "Git status and repository initialization are already used by the `Source Control` rail.",
                "PTY terminal lifecycle is already wired into the bottom-dock terminal surfaces.",
                "`/api/coordination` provides workspace-scoped agent registration, credential-protected heartbeat/disconnect, hierarchical read/write leases, FIFO conflict queues, writer fairness, expiry cleanup, and state inspection for `sf-mcp` clients.",
                "Non-overlapping resources and separate workspaces can proceed concurrently; overlapping writes plus `build` or `runtime` exclusivity are coordinated without one global bridge lock.",
                "Coordination credentials are returned only at registration and are excluded from state, lease, SSE event, log, and error views."
              ]
            },
            {
              "title": "Shell Usage",
              "keywords": ["workspaces rail", "explorer rail", "source control rail"],
              "items": [
                "Create or edit workspaces from the `Workspaces` rail.",
                "Browse files and preview text content from the `Explorer` rail.",
                "Inspect branch state and working tree changes from `Source Control`.",
                "Open `Activity` in the bottom dock to inspect durable operation provenance and selected exact text changes, then approve or reject pending reviews."
              ]
            }
          ]
        },
        {
          "id": "shader-forge-mcp",
          "title": "Shader Forge MCP",
          "summary": "Shader Forge MCP (`sf-mcp`) is the process-scoped stdio adapter for read access, engine-owned multi-agent leases, and lease-gated spatial attachment plus whole scene/prefab mutation workflows without embedding an assistant in the editor.",
          "keywords": ["shader forge mcp", "sf-mcp", "mcp", "stdio", "codex", "grok", "antigravity", "multi-agent", "lease", "coordination", "heartbeat", "spatial attachment", "operation"],
          "references": [
            "tools/engine-mcp/server.mjs",
            "docs/specs/ENGINE-MCP-SPEC.md",
            "docs/specs/ENGINE-SESSIOND-SPEC.md",
            "docs/specs/ENGINE-OPERATIONS-SPEC.md",
            "docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md",
            "docs/guides/SHADER-FORGE-MCP-SETUP.md",
            "scripts/test-engine-mcp.mjs"
          ],
          "sections": [
            {
              "title": "Startup And Lifecycle",
              "keywords": ["root", "session", "base url", "name", "credential", "disconnect"],
              "items": [
                "The current adapter is a process-scoped stdio server. Each Codex, Grok CLI, Google Antigravity, or other MCP client launches its own `sf-mcp` process.",
                "Start it with `--root <path>` for stable project selection or `--session <id>` for an existing backend session; `--base-url <url>` selects `engine_sessiond`, and `--name <client-name>` identifies the client process.",
                "`.agents/mcp_config.json` gives Antigravity a portable workspace registration using repository-relative server/root paths and `cwd`; start `engine_sessiond` before reloading it in Antigravity's MCP manager.",
                "Stdout is reserved for MCP protocol messages and diagnostics go to stderr.",
                "Each process registers one workspace-scoped coordinator agent, keeps the returned credential private in process memory, heartbeats while connected, and disconnects on shutdown."
              ]
            },
            {
              "title": "Resources And Tools",
              "keywords": ["project", "files", "coordination state", "work lease", "operation validation"],
              "items": [
                "The current resources are `shaderforge://project`, `shaderforge://coordination`, and the typed `shaderforge://spatial/review/{reviewId}` template.",
                "Current read tools are `project_status`, read-only/idempotent `project_diagnostics`, `project_files_list`, `project_file_read`, `coordination_state`, read-only/idempotent `spatial_attachment_read`, and immutable `spatial_review_read`; `project_diagnostics` reuses the selected workspace's live profile summary.",
                "Current coordination tools are `work_lease_request`, `work_lease_status`, `work_lease_release`, and `agent_heartbeat`.",
                "Current operation tools are bounded `operation_list`, `operation_read`, strict whole scene/prefab `scene_asset_preview`, lease-free `spatial_attachment_validate`, `spatial_attachment_preview`, explicit `spatial_review_reserve` / `spatial_review_recapture`, separate `operation_approve` / `operation_reject`, and semantic `operation_apply` / `operation_undo`.",
                "The MCP actor and session come from process state. Validation first rejects foreign-session operations and sends no credential or lease. Preview/apply/undo require the process-owned agent/credential and an owned granted write lease; recapture requires explicit owned source/capture/review leases; sessiond rechecks authority immediately before mutation or publication.",
                "Separate Codex, Grok, and Antigravity processes can inspect the same workspace concurrently and receive non-overlapping leases, while conflicting hierarchical work queues through `engine_sessiond`."
              ]
            },
            {
              "title": "Current Safety Boundary",
              "keywords": ["read only", "mutation", "approval", "revision", "http", "assistant"],
              "items": [
                "The mutation surface is limited to semantic spatial attachments and complete validated scene/prefab assets. Generic file/entity/component/code writes, build/runtime mutation, arbitrary commands, and HTTP transport remain excluded.",
                "`sf-mcp` never calls `/api/files/write`, accepts caller-provided identity or credentials, or automatically acquires authority, approves, retries, applies, or undoes. Successful recapture releases its three purpose-specific leases through the authoritative sessiond contract; ordinary leases remain explicit. Structured 409 results preserve safe conflict data plus a refreshed authoritative operation when available.",
                "A granted lease coordinates ownership but does not grant authority to bypass engine mutation policy.",
                "Shader Forge MCP does not contain model execution, provider selection, assistant chat, or prompt UI; its bounded AI-readiness diagnostics are read-only, and external clients own the AI experience.",
                "Run `npm run test:mcp` to launch through the Antigravity workspace command/cwd shape and verify deterministic stdio, boundary, coordination, revision-bound spatial rest/sample reads, full spatial and review operation/resource coverage, structured conflicts, and disconnect cleanup."
              ]
            }
          ]
        },
        {
          "id": "engine-cli-surfaces",
          "title": "Engine CLI Surfaces",
          "summary": "The current CLI now covers backend bring-up, runtime bring-up, AI provider/status/test, export/package inspection, profiling snapshots, a first asset bake lane, fixture-backed migration detection/conversion/reporting, and explicit audio-root, animation-root, plus physics-root wiring rather than only shell support commands.",
          "keywords": ["engine", "commands", "build runtime", "run", "bake", "cook", "package", "export", "profile", "procgeo", "audio", "animation", "physics", "migrate", "migration", "unity", "unreal", "godot", "offline fallback", "blueprint", "ai", "ollama", "openrouter", "kimi"],
          "references": [
            "tools/engine-cli/shaderforge.mjs",
            "tools/shared/engine-ai-service.mjs",
            "tools/engine-cli/lib/asset-pipeline.mjs",
            "tools/engine-cli/lib/migration-foundation.mjs",
            "docs/specs/ENGINE-CLI-SPEC.md",
            "docs/specs/ENGINE-ASSET-PIPELINE-SPEC.md",
            "docs/specs/ENGINE-AUDIO-SPEC.md",
            "docs/specs/ENGINE-AI-SPEC.md",
            "docs/specs/ENGINE-MIGRATION-SPEC.md"
          ],
          "sections": [
            {
              "title": "Implemented Commands",
              "keywords": ["sessiond start", "session create", "build", "run", "bake", "migrate", "ai"],
              "items": [
                "`engine sessiond start` starts the local backend service.",
                "`engine session create` and `engine session list` expose session bring-up from the terminal.",
                "`engine file list` and `engine file read` expose safe file inspection.",
                "`engine ai providers [--root <path>]` now prints the workspace AI provider manifest, default provider, readiness state, and diagnostics.",
                "`engine ai test [--root <path>] [--provider <id>] [--prompt <text>] [--system <text>]` runs the shared deterministic fake, optional Ollama, or enabled OpenRouter smoke-test lane and reports normalized token usage when supplied by a real provider.",
                "`engine ai request <prompt> [--root <path>] [--provider <id>] [--system <text>]` reuses that path for freeform prompts; `openrouter_kimi` and `openrouter_glm` are disabled by default and use `OPENROUTER_API_KEY`, the `moonshotai/kimi-k3` or `z-ai/glm-5.2` model slug, and a validated `max_output_tokens` ceiling when enabled.",
                "`engine ai submit <prompt>`, `jobs`, `status <job-id>`, and `cancel <job-id>` are thin adapters over the process-scoped sessiond queue; use `--session` to bind submit/list operations to a workspace and `--base-url` when sessiond is not on its default loopback address.",
                "`engine export inspect [--root <path>] [--preset <id>] [--package-root <path>]` prints the resolved preset, path readiness, cooked-asset counts, last-package summary, and whether its safely bounded archive still exists; `default` and `release` provide Debug and Release desktop configurations.",
                "`engine package [--root <path>] [--preset <id>] [--package-root <path>] [--skip-bake] [--force-bake]` emits the release layout under `build/package/<preset>/`, auto-bakes missing cooked outputs unless skipped, and executes the default `archive_zip` hook to write `build/package/<preset>.zip`; unsupported hooks remain `declared_only` in the report.",
                "`engine profile live [--root <path>]` now prints the first diagnostics snapshot lane, and `--session` plus `--base-url` can switch that to a live `engine_sessiond` snapshot.",
                "`engine profile list [--root <path>] [--session <id>] [--base-url <url>] [--limit <count>]` now lists persisted diagnostics captures from either a workspace or a live `engine_sessiond` session.",
                "`engine profile capture [--root <path>] [--label <name>] [--output <path>]` now writes a shareable JSON diagnostics capture under `build/profiling/captures/`, and `--session` plus `--base-url` can capture a live sessiond-backed snapshot with recent runtime/build logs.",
                "`engine policy inspect [--root <path>]` now prints the effective code-trust policy, supported authored hot-reload roots, and tracked artifacts for a workspace.",
                "`engine policy check <action> [path] [--root <path>] [--actor ...] [--origin ...]` now dry-runs the same code-trust layer that sessiond enforces before risky assistant-facing transitions.",
                "`engine policy artifacts [--root <path>]` now prints tracked artifact hashes, verification state, and promote/quarantine metadata for a workspace.",
                "`engine policy approvals [--session <id>] [--state pending|all] [--base-url <url>]` now lists pending or historical code-trust approvals from a running backend.",
                "`engine policy approve <approval-id>` and `engine policy deny <approval-id>` now resolve queued code-trust approvals from the terminal.",
                "`engine policy promote <path> [--session <id>] [--root <path>] [--base-url <url>] [--decision-by <name>] [--note <text>]` now promotes a tracked artifact through `engine_sessiond` and refreshes its trusted hash.",
                "`engine policy quarantine <path> [--session <id>] [--root <path>] [--base-url <url>] [--decision-by <name>] [--note <text>]` now quarantines a tracked artifact through that same sessiond mutation authority so later risky transitions keep denying it until it is explicitly promoted again.",
                "`engine build runtime` configures and builds the native runtime plus the `shader_forge_data` validator with CMake; `engine build data` builds only that validator; `engine build spatial` builds the dependency-free native spatial validate/cook/evaluate-rest/evaluate-sample tool. All resolve CMake from `SHADER_FORGE_CMAKE` first and then `cmake` on `PATH`.",
                "`engine spatial validate [--animation-root animation] [--build-dir build/runtime] [--config Debug]` runs the already-built validator and prints deterministic JSON. It gives a build-first error when absent and does not compile, cook, or start a daemon.",
                "`engine spatial cook [--animation-root animation] [--output-root build/cooked] [--build-dir build/runtime] [--config Debug]` validates through the same `AnimationSystem` and atomically stages one byte-stable derived payload at `<output-root>/animation/spatial-authoring.bin`. It does not auto-build, start a daemon, or join generic `engine bake` yet.",
                "`engine spatial evaluate-rest --attachment <id> [--animation-root animation] [--content-root content] [--data-foundation data/foundation/engine-data-layout.toml] [--build-dir build/runtime] [--config Debug]` prints deterministic rest frames, exact prefab-bound render-procgeo visual-box corners when available, diagnose-only joint-limit evidence, and diagnose-only capsule-axis-to-authored-collision-OBB surface clearance. Solved IK is unavailable; the result is neither runtime collision nor review evidence.",
                "`engine spatial evaluate-sample --attachment <id> --phase <phase> --normalized-time <value> [--animation-root animation] [--content-root content] [--data-foundation data/foundation/engine-data-layout.toml] [--build-dir build/runtime] [--config Debug]` requires an exact authored envelope sample and carries the same independent visual-box and collision/capsule diagnostic evidence. V2 applies deterministic palm-effector IK before joint-limit and clipping evaluation; v1 remains pre-IK; every result remains non-review schematic evidence.",
                "`engine spatial preview --session <id> --path animation/attachments/<file>.attachment.toml --content-file <path> --base-revision <sha256:...|missing> --label <text> --agent <id> --lease <id> [--base-url <url>]` sends a strict BOM-free UTF-8 full-content candidate to sessiond without mutating authored bytes.",
                "`engine spatial validate-operation <operation-id> --samples-file <path> [--base-url <url>]` sends a strict BOM-free UTF-8 top-level JSON sample array unchanged to the lease-free sessiond operation route without a coordinator credential.",
                "`engine spatial approve|reject <operation-id>` and `engine spatial apply|undo <operation-id> --agent <id> --lease <id>` call the existing operation transitions. Apply/undo require the credential in `SHADER_FORGE_AGENT_CREDENTIAL`; no command performs implicit coordination or approval.",
                "`engine run <scene>` builds and launches the native runtime and now forwards content, audio, animation, physics, data, save, and tooling roots.",
                "`engine bake` scans text-backed content, audio, animation, and physics roots, emits staged cooked outputs into `build/cooked/`, and writes a deterministic asset-pipeline report.",
                "`engine migrate detect|unity|godot <path>` now emits normalized migration manifests plus report/warning outputs for supported source-engine project shapes.",
                "`engine migrate unreal <path>` now reports the explicit `unreal_offline_fallback` lane, lower conversion confidence, and low-confidence Blueprint package manifests when exporter-assisted Unreal data is unavailable in the current slice.",
                "`engine migrate report <path>` summarizes a generated migration report, including mapped scene-entity, prefab-component, and script-binding coverage, from the terminal."
              ]
            },
            {
              "title": "Current Non-Goals",
              "keywords": ["import", "bake", "migration", "package", "export", "ai"],
              "items": [
                "`engine import` is still a later phase.",
                "`ai/providers.toml` defines deterministic `fake`, optional Ollama, and disabled-by-default OpenRouter/Kimi K3 plus GLM 5.2 lanes. Real requests default to a 256-token output ceiling and return normalized provider usage when present; OpenRouter validates 1–4096, pins its official API root/key name, rejects remote redirects, caps responses at 1 MiB, never returns credentials, and fails closed on unknown provider types or explicit IDs.",
                "`npm run test:ai-scaffold` is the deterministic harness for provider inspection/execution plus queued status, pending/running cancellation, and recovery.",
                "The CLI bake lane is now real, but it still emits staged cooked payloads and generated-mesh previews rather than the final FlatBuffers writer.",
                "The CLI packaging lane is now real, but it still packages authored runtime roots plus bundled cooked outputs rather than a final cooked-runtime shipping layout.",
                "The CLI profiling lane is now real, but it still captures JSON diagnostics snapshots rather than Tracy, RenderDoc, or native in-process profiling panels.",
                "The CLI audio lane is now real for authored metadata and staged cook outputs, but it does not drive real playback yet.",
                "The CLI migration lane is now split honestly: `detect` is report-only, Unity and Godot emit first-pass Shader Forge project skeletons, and Unreal currently records the explicit `unreal_offline_fallback` lane instead of claiming exporter-assisted parity."
              ]
            }
          ]
        },
        {
          "id": "migration-foundation",
          "title": "Migration Foundation",
          "summary": "The current migration slice is a detect/report foundation plus fixture-backed project-skeleton, exact startup-setting, and Unity/Godot text-scene hierarchy/transform conversion, alongside the explicit Unreal offline fallback lane.",
          "keywords": ["migration", "detect", "report", "manifest", "provenance", "startup scene", "bootstrap", "fixtures", "unity hierarchy", "godot hierarchy", "mapped scene entities", "offline fallback", "unreal", "blueprint"],
          "references": [
            "tools/engine-cli/lib/migration-foundation.mjs",
            "fixtures/migration/unity-minimal/ProjectSettings/ProjectVersion.txt",
            "fixtures/migration/unity-minimal/ProjectSettings/EditorBuildSettings.asset",
            "fixtures/migration/unreal-minimal/ExampleProject.uproject",
            "fixtures/migration/unreal-offline-minimal/ExampleOfflineProject.uproject",
            "fixtures/migration/godot-minimal/project.godot",
            "scripts/test-engine-migration-fixtures.mjs",
            "docs/specs/ENGINE-MIGRATION-SPEC.md"
          ],
          "sections": [
            {
              "title": "Implemented Now",
              "keywords": ["unity", "unreal", "godot", "report root"],
              "items": [
                "`engine migrate detect` auto-detects Unity, Unreal, or Godot project structure and writes `migration-manifest.toml`, `report.toml`, and `warnings.toml` under `migration/<run-id>/`.",
                "`engine migrate unity` and `engine migrate godot` now pin the requested source-engine lane while also emitting a self-contained `shader-forge-project/` skeleton under each run root.",
                "`engine migrate unreal` now pins the explicit `unreal_offline_fallback` lane, records the preferred exporter-assisted lane separately, and emits the same first-pass target-project skeleton shape.",
                "Pinned engine lanes now emit first-pass scene, prefab, bootstrap-data, and script-porting outputs for the current fixtures.",
                "Startup selection maps Unity's first enabled `ProjectSettings/EditorBuildSettings.asset` scene, Unreal's `[/Script/EngineSettings.GameMapsSettings].GameDefaultMap`, and Godot's `[application].run/main_scene` when it uses `res://`, resolving against exact source-project-relative scene records.",
                "Each pinned conversion emits a `[startup_scene]` source/target provenance record plus converted, approximated, and skipped project-setting counts. Duplicate scene basenames receive deterministic source-path-derived target names.",
                "Migration CLI output, manifests, and reports expose separate `mapped_scene_entities`, `mapped_prefab_components`, and `mapped_script_bindings` coverage counts.",
                "Unity text-YAML scene conversion maps active `GameObject` plus `Transform`/`RectTransform` documents into parent-linked entities and scene-owned prefabs, preserves source file IDs, carries local position/quaternion rotation/scale into reviewable generated records, and maps enabled valid perspective `Camera` optics plus enabled valid default-layer non-trigger `BoxCollider` center/size geometry without PhysicMaterial references into strict prefab components. `m_IsActive: 0` objects and their hierarchy subtrees are omitted; disabled or semantically unsupported components fail closed instead of becoming active target components.",
                "Unity `MonoBehaviour` script GUIDs resolve through `.cs.meta` files into binding-specific script-porting manifests that preserve source scene, node path, GameObject/component IDs, script path, and confidence; component fields and C# behavior are not translated.",
                "Godot node script `ExtResource` references resolve safe `res://` paths into binding-specific script-porting manifests that preserve source scene, node path, resource ID/path, script path, and confidence; node fields and GDScript behavior are not translated.",
                "Godot text-scene conversion maps declared nodes into parent-linked entities, carries explicit position/rotation/scale into Shader Forge transforms, maps valid explicit perspective `Camera3D` optics and enabled non-`Area3D` `CollisionShape3D`-referenced `BoxShape3D` geometry, derives prefab tags from node types, retains source comments, reports mapped coverage, and deterministically disambiguates normalized node-name collisions. `disabled = true` and direct `Area3D` trigger collision shapes fail closed.",
                "Binary Godot `.scn` files retain a reviewable placeholder but are not parsed or counted as mapped scene entities.",
                "An explicit startup scene that does not resolve is fail-closed: no `runtime_bootstrap.data.toml` is emitted, the setting is `skipped`, and warnings/manual work identify the unresolved value. With no declaration, the first source-relative converted scene is selected deterministically and marked `approximated`.",
                "`asset_conversion` is `Manual` until source payload import exists. The migration fixture harness feeds emitted scene, prefab, and optional bootstrap records through the production asset-pipeline baker."
              ]
            },
            {
              "title": "Current Boundaries",
              "keywords": ["foundation only", "manual follow-up", "conversion"],
              "items": [
                "The current migration slice now converts project structure into a usable Shader Forge skeleton, but not full runtime parity.",
                "The Unreal offline fallback currently derives scene and prefab placeholders from `.umap` names, Blueprint-like `.uasset` package names, and available C++ class symbols rather than exported Unreal actor data.",
                "Art payloads, materials, Unity prefab instances/other components/component fields and behavior/disabled camera and collider plus trigger-collider preservation/orthographic cameras/layer-material collider semantics/coordinate-system remediation, Unreal hierarchy, Godot transform matrices/resource instances/other components/script behavior and fields/camera enabled-current semantics plus physics-body, disabled-collider-preservation, layer, and material semantics, gameplay translation, conflict-safe reimport, and exporter-assisted Unreal extraction still require later migration passes.",
                "The run output must keep calling out approximations and manual follow-up instead of implying parity."
              ]
            }
          ]
        }
      ]
    },
    {
      "id": "runtime-and-authoring",
      "title": "Runtime And Authoring",
      "description": "The native runtime is past pure scaffolding, and the content data lanes now affect runtime startup behavior for real.",
      "keywords": ["runtime", "vulkan", "scene", "prefab", "data", "bootstrap", "spatial authoring", "attachment"],
      "pages": [
        {
          "id": "runtime-and-viewer",
          "title": "Runtime Bring-Up And Viewer Bridge",
          "summary": "The runtime opens a real SDL3/Vulkan window when local dependencies exist, while the shell owns build/run orchestration and diagnostics.",
          "keywords": ["viewer", "preview", "external window", "swapchain"],
          "references": [
            "engine/runtime/include/shader_forge/runtime/save_system.hpp",
            "engine/runtime/src/runtime_app.cpp",
            "engine/runtime/src/save_system.cpp",
            "shell/engine-shell/src/App.tsx",
            "tools/engine-sessiond/server.mjs",
            "docs/specs/ENGINE-RUNTIME-SPEC.md",
            "docs/specs/ENGINE-RENDERER-SPEC.md",
            "docs/specs/ENGINE-SAVE-SYSTEM-SPEC.md"
          ],
          "sections": [
            {
              "title": "Native Runtime",
              "keywords": ["sdl3", "vulkan", "render pass", "present"],
              "items": [
                "The native runtime has Vulkan instance, surface, device, swapchain, render pass, framebuffer, submit, and present bring-up.",
                "Resize-aware swapchain recreation is already implemented.",
                "Runtime build, run, stop, restart, pause, and resume are already controlled from the shell, and shell-driven run/restart now follow the active session root.",
                "`Playtest` keeps the current world, runtime state, and valid primary actions visible. Build state, bridge activity, paths, and recent log tails are available under collapsed `Diagnostics`; build and runtime failures remain visible without opening it.",
                "The native runtime now also composes the authored scene into a first runtime world snapshot, selects a preferred controlled entity from prefab spawn tags, and projects authored prefab render components into visible debug-proxy scene cards in the external Vulkan window.",
                "The native runtime now also has a first authored-content iteration lane: `F7` forces reload of content/audio/animation/physics/data state, and the runtime also polls saved authored-file timestamps to pick up shell edits without a full restart.",
                "The native runtime now resolves effect-capable interaction targets from the current view/crosshair, and `ui_accept` input such as Enter or left-click triggers first visible interaction feedback plus effect-descriptor-backed logs.",
                "The native runtime now also has a widened first save-system lane: `F8` writes the active `quickslot_01` through `quickslot_03` runtime save, `F9` reloads it, `F11`/`F12` cycle the active slot, and the save path follows the active session/project root instead of mixing runtime persistence into authored content roots.",
                "The native runtime now also has a first projected physics-debug lane: authored blocking bodies and query-only trigger bodies can be visualized in the external window, overlap-triggered bodies are highlighted, and `F10` toggles that view during manual testing."
              ]
            },
            {
              "title": "Current Viewer Boundary",
              "keywords": ["embedded viewer", "screenshots", "browser shell"],
              "items": [
                "The native runtime still renders in an external window.",
                "The browser shell remains the primary workspace.",
                "Embedded viewer transport and screenshot capture are still deferred until the native/runtime side is worth the added complexity.",
                "On Windows, Visual Studio can provide CMake without providing the SDL3 development package or Vulkan SDK that the real native runtime needs, so a successful build and launch can still end in stub-mode runtime exit until those native dependencies are installed."
              ]
            }
          ]
        },
        {
          "id": "scene-and-content-foundation",
          "title": "Scene, Prefab, And Data Foundation",
          "summary": "Text-backed content roots now exist and runtime startup resolves scene/bootstrap metadata from them instead of treating scene names as free text only, while the shell now author-reads and author-writes the same scene/prefab roots and the first bake lane targets those roots plus dedicated audio, animation, and physics lanes.",
          "keywords": ["content", "scene", "prefab", "data foundation", "effects", "procgeo", "audio", "animation", "physics", "bake"],
          "references": [
            "engine/runtime/include/shader_forge/runtime/data_foundation.hpp",
            "engine/runtime/include/shader_forge/runtime/animation_system.hpp",
            "engine/runtime/include/shader_forge/runtime/audio_system.hpp",
            "engine/runtime/include/shader_forge/runtime/physics_system.hpp",
            "engine/runtime/include/shader_forge/runtime/save_system.hpp",
            "engine/runtime/src/animation_system.cpp",
            "engine/runtime/src/audio_system.cpp",
            "engine/runtime/src/data_foundation.cpp",
            "engine/runtime/src/physics_system.cpp",
            "engine/runtime/src/save_system.cpp",
            "content/scenes/sandbox.scene.toml",
            "content/prefabs/debug_camera.prefab.toml",
            "shell/engine-shell/src/SceneEditorView.tsx",
            "shell/engine-shell/src/scene-authoring.ts",
            "tools/engine-sessiond/lib/session-store.mjs",
            "content/data/runtime_bootstrap.data.toml",
            "content/procgeo/sandbox_floor.procgeo.toml",
            "audio/buses.toml",
            "audio/sounds/ui_confirm.sound.toml",
            "audio/events/runtime_boot.audio-event.toml",
            "animation/skeletons/debug_humanoid.skeleton.toml",
            "animation/clips/debug_walk.anim.toml",
            "animation/graphs/debug_actor.animgraph.toml",
            "physics/layers.toml",
            "physics/materials/default_surface.physics-material.toml",
            "physics/bodies/debug_crate.physics-body.toml",
            "data/foundation/engine-data-layout.toml",
            "tools/engine-cli/lib/asset-pipeline.mjs",
            "docs/specs/ENGINE-DATA-SPEC.md",
            "docs/specs/ENGINE-SCENE-SPEC.md",
            "docs/specs/ENGINE-VFX-SPEC.md",
            "docs/specs/ENGINE-PROCGEO-SPEC.md",
            "docs/specs/ENGINE-AUDIO-SPEC.md",
            "docs/specs/ENGINE-ANIMATION-SPEC.md",
            "docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md",
            "docs/specs/ENGINE-PHYSICS-SPEC.md",
            "docs/specs/ENGINE-ASSET-PIPELINE-SPEC.md",
            "docs/specs/ENGINE-SAVE-SYSTEM-SPEC.md"
          ],
          "sections": [
            {
              "title": "Current Content Roots",
              "keywords": ["assets", "toml", "authored content"],
              "items": [
                "`content/scenes/*.scene.toml` is the initial authored scene lane.",
                "`content/prefabs/*.prefab.toml` is the initial authored prefab lane.",
                "`content/data/*.data.toml` is the initial authored engine/bootstrap data lane.",
                "`content/effects/*.effect.toml` is the initial authored effect-descriptor lane.",
                "`content/procgeo/*.procgeo.toml` is the initial authored procedural-geometry lane; dimensions require complete unquoted finite positive float32 numbers, plane-grid row/column counts require complete decimal integers, and generated grids are limited to 65,536 vertices.",
                "`audio/buses.toml`, `audio/sounds/*.sound.toml`, and `audio/events/*.audio-event.toml` are the initial authored audio lanes.",
                "`animation/skeletons/*.skeleton.toml`, `animation/clips/*.anim.toml`, and `animation/graphs/*.animgraph.toml` are the initial authored animation lanes.",
                "`AnimationSystem` supports an optional `animation/attachments/*.attachment.toml` project lane; current attachment profiles remain isolated validation fixtures under `animation/fixtures/spatial/attachments/`.",
                "`physics/layers.toml`, `physics/materials/*.physics-material.toml`, and `physics/bodies/*.physics-body.toml` are the initial authored physics lanes.",
                "`saved/runtime/*.runtime-save.toml` is now the initial runtime-persistence lane and is intentionally separate from authored source assets."
              ]
            },
            {
              "title": "Runtime Data Foundation And Bake Lane",
              "keywords": ["flatbuffers", "sqlite", "catalog", "bootstrap", "engine bake", "generated meshes", "audio events", "animation graphs", "physics queries"],
              "items": [
                "`data/foundation/engine-data-layout.toml` defines the current `TOML -> FlatBuffers -> SQLite` split.",
                "The runtime validates the content roots through `DataFoundation` before startup continues.",
                "Scene-to-prefab relationships are validated across the catalog.",
                "Scene entity position, rotation, and scale use strict quoted three-number vectors; catalog loading and baking reject trailing junk, radix notation, non-finite values, float32 overflow, and subnormal values.",
                "Prefabs may optionally author one strict perspective `[component.camera]` with float32-compatible vertical FOV and near/far clips. A scene may reference at most one root or parented `player_camera`; malformed lines, unknown `component.*` tables, duplicate camera fields/tables, invalid bounds, and invalid hierarchy fail validation and cook.",
                "Camera validation and staged camera cooking use the same native float32 values, so authored optics cannot change when the final FlatBuffers writer replaces the current JSON staging format.",
                "The runtime composes authored scene entities plus prefab payloads into a runtime scene snapshot with true parent/local TRS world transforms: scaled local translation is parent-rotated and added to parent position, rotations compose, and scales multiply component-wise.",
                "The runtime can now look up authored procgeo dimensions and use them to size projected debug proxies for prefab render components in the scene viewer.",
                "`runtime_bootstrap.data.toml` can now provide a default scene and tooling overlay preference.",
                "The runtime window title and startup logs now include active scene and primary prefab context from the authored assets.",
                "The runtime now selects a preferred controlled entity from authored spawn tags such as `player_camera`, and `move_*` plus `look_*` input now drives that entity state.",
                "A valid root or parented `player_camera` takes priority over `player_spawn` and drives projection from its composed world transform plus authored camera values. Ordinary scenes keep the legacy 70-degree, 0.15-meter near, and 1000-meter far defaults. Quickload rejects stale controlled authority instead of silently changing camera semantics.",
                "Controlled-entity movement now respects a first authored-physics blocking lane against scene physics bodies, and the runtime surfaces the blocking body in logs plus window state during manual testing.",
                "Authored `on_overlap` effect triggers can now activate automatically from query-only scene bodies during runtime movement, so the running scene has a first automatic trigger-volume lane alongside manual `ui_accept` interaction.",
                "The shell `World` workspace opens revision-bearing scene and prefab assets directly from the active session root and round-trips deterministic save, reload, revert, duplicate, and primary-object edits through the semantic scene-asset operation lane.",
                "Shell run/restart now forward the active session root into runtime launch so the external runtime reads the same authored scene files the shell edits.",
                "The running runtime now follows those authored edits through a first polling/manual reload lane rather than requiring a full process restart for every save.",
                "Effect-capable proxies in the running scene can now be aimed at with the crosshair and triggered through `ui_accept`, which surfaces first runtime feedback for authored `[component.effect]` data instead of leaving it as static catalog metadata.",
                "`saved/runtime/quickslot_01.runtime-save.toml` through `quickslot_03.runtime-save.toml` are now the first inspectable runtime save payloads, and the current snapshot stores scene, controlled-entity, transform, animation-context, and triggered-overlap state for manual iteration.",
                "`Edit` and `Verify` share the same in-memory World drafts. `Verify` is read-only and never discards unsaved work; returning to `Edit` restores write controls without rebuilding the draft.",
                "The runtime now loads authored audio buses, sounds, and named events through `AudioSystem`.",
                "Runtime startup resolves a `runtime_boot` audio event, and `ui_accept` now flows through the same engine-owned audio event API.",
                "The runtime now loads authored animation skeletons, clips, and graphs through `AnimationSystem`.",
                "Runtime startup resolves a default animation graph, logs graph/state/event catalog data, and routes entry-clip `audio_event` hooks through the engine-owned audio event API.",
                "Movement now drives a first authored animation-state lane in runtime: `idle` and `walk` can be resolved by name from the current graph, the active state/clip is surfaced in window state, and walk clip `audio_event` hooks now fire during movement playback.",
                "The native tooling overlay now also surfaces live player id/position, movement speed, active animation state/clip, blocking body, active save slot, current interaction target, active triggered effect, and physics-debug state so manual runtime testing is not dependent on log scanning alone.",
                "The runtime now loads authored physics layers, materials, and primitive bodies through `PhysicsSystem`.",
                "Runtime startup logs physics layer/body summaries and runs deterministic raycast plus overlap queries against the active scene.",
                "The current runtime slice can now project first physics-debug body visualization for blocking and query-only bodies, but it is still a debug overlay rather than final in-engine physics gizmos.",
                "`engine bake` now emits staged cooked outputs into `build/cooked/`, writes generated-mesh preview payloads for `procgeo` assets, stages cooked audio metadata under `build/cooked/audio/`, stages cooked animation metadata under `build/cooked/animation/`, and stages cooked physics metadata under `build/cooked/physics/`."
              ],
              "note": "This is still a widening slice. The bake path is real, but it still uses staged placeholder cooked payloads until the FlatBuffers writer and runtime scene loading land."
            },
            {
              "title": "Audio Foundation",
              "keywords": ["buses", "sounds", "events", "playback"],
              "items": [
                "`audio/buses.toml` defines the initial required buses: `Master`, `Music`, `SFX`, `Voice`, and `Ambience`.",
                "`audio/sounds/*.sound.toml` defines named sounds with bus routing, playback mode, spatialization, streaming, and default volume metadata.",
                "`audio/events/*.audio-event.toml` defines named audio events that currently resolve to sound-play requests through engine-owned APIs.",
                "The current audio slice validates and resolves requests, but it does not decode or mix sound yet. Playback backend integration is still ahead."
              ]
            },
            {
              "title": "Animation Foundation",
              "keywords": ["skeletons", "clips", "graphs", "root motion", "events"],
              "items": [
                "`animation/skeletons/*.skeleton.toml` defines named authored skeletons with root-bone and bone-list metadata.",
                "The current `debug_humanoid` skeleton is three-bone metadata only: `hips, spine, head`. There is no parent table, rest pose, semantic role map, or socket list.",
                "`animation/clips/*.anim.toml` defines named clips with skeleton ownership, looping/root-motion metadata, and text-backed clip events.",
                "`animation/graphs/*.animgraph.toml` defines named animation graphs with float parameters, named states, and explicit entry-state selection.",
                "`AnimationSystem` now strictly validates v2 skeleton hierarchies, roles, sockets, and canonical quaternions plus v1 attachment profiles while keeping the existing v1 skeleton lane compatible.",
                "Every animation source path and file must be valid UTF-8 before `AnimationSystem` commits a new generation.",
                "Generation-tagged skeleton, bone, socket, and attachment handles invalidate after a successful reload; a failed reload retains the prior valid generation and snapshots.",
                "The current runtime slice validates and resolves default graphs plus entry-clip events. Deterministic schema-v2 clip sampling exists as a native query, but runtime graph playback still does not consume it, blend, or retarget animation."
              ]
            },
            {
              "title": "Physics Foundation",
              "keywords": ["layers", "materials", "bodies", "raycast", "overlap"],
              "items": [
                "`physics/layers.toml` defines the initial collision layers and text-backed collision masks.",
                "`physics/materials/*.physics-material.toml` defines named physics materials with friction, restitution, and density metadata.",
                "`physics/bodies/*.physics-body.toml` defines primitive scene bodies with scene ownership, layer/material references, motion type, and box or sphere shape data.",
                "The current runtime slice validates and resolves deterministic raycast and overlap queries over those primitive bodies, but it does not run a full simulation backend yet."
              ]
            }
          ]
        },
        {
          "id": "spatial-authoring",
          "title": "Spatial Authoring And Attachment Tuning",
          "summary": "Spatial authoring now has a native schema/query/cook foundation, deterministic rest/sampled evaluation with joint-limit and capsule/item surface-clearance diagnostics, deterministic native close/player-camera rendering, lease-free operation validation through CLI/MCP, native apply/undo revalidation, lease-gated sessiond recapture with immutable packet/capture reads, CLI/MCP review adapters and a typed review resource, constrained Assets schematics, packet-referenced Assets review presentation, and an authored runtime camera.",
          "keywords": [
            "spatial authoring",
            "attachment",
            "socket",
            "review packet",
            "reviewId",
            "grip",
            "two-hand",
            "IK",
            "preview-candidate",
            "shader_forge_spatial",
            "engine spatial validate",
            "engine spatial cook",
            "engine spatial evaluate-sample",
            "capture-sample",
            "recapture",
            "spatial-reviews"
          ],
          "references": [
            "docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md",
            "docs/specs/ENGINE-ANIMATION-SPEC.md",
            "docs/specs/ENGINE-OPERATIONS-SPEC.md",
            "docs/specs/ENGINE-SESSIOND-SPEC.md",
            "docs/specs/ENGINE-MCP-SPEC.md",
            "docs/specs/ENGINE-RUNTIME-SPEC.md",
            "docs/specs/ENGINE-SHELL-SPEC.md",
            "docs/specs/ENGINE-ASSET-PIPELINE-SPEC.md",
            "docs/specs/ENGINE-SCENE-SPEC.md",
            "engine/runtime/include/shader_forge/runtime/animation_system.hpp",
            "engine/runtime/include/shader_forge/runtime/spatial_capture.hpp",
            "engine/runtime/src/animation_system.cpp",
            "engine/runtime/src/spatial_capture.cpp",
            "engine/runtime/src/spatial_authoring_tool.cpp",
            "tools/engine-sessiond/lib/spatial-attachment-service.mjs",
            "tools/engine-sessiond/lib/coordination-store.mjs",
            "tools/engine-sessiond/lib/session-store.mjs",
            "tools/engine-sessiond/server.mjs",
            "tools/engine-cli/shaderforge.mjs",
            "tools/engine-mcp/server.mjs",
            "animation/skeletons/debug_humanoid.skeleton.toml",
            "animation/fixtures/spatial/skeletons/spatial_humanoid.skeleton.toml",
            "animation/fixtures/spatial/attachments/rifle_mk1_humanoid.attachment.toml",
            "scripts/test-engine-spatial-authoring-scaffold.mjs",
            "scripts/test-engine-spatial-tool.mjs",
            "scripts/test-engine-spatial-operations.mjs",
            "scripts/test-engine-spatial-cli.mjs",
            "scripts/test-engine-spatial-shell.mjs",
            "shell/engine-shell/src/SpatialAttachmentEditorView.tsx",
            "shell/engine-shell/src/SpatialRestSchematic.tsx",
            "shell/engine-shell/src/spatial-attachment-authoring.ts",
            "plans/ENGINE-IMPLEMENTATION-PLAN.md",
            "plans/SHADER-FORGE-REDESIGN-UPLIFT-PLAN.md"
          ],
          "sections": [
            {
              "title": "Authored-Truth Contract",
              "keywords": ["native code", "typed assets", "sessiond", "preview-candidate", "cooked data"],
              "items": [
                "Native engine code owns behavior, typed schemas, validation, and runtime execution.",
                "Source-controlled typed assets own project tuning values.",
                "Shell, CLI, and `sf-mcp` edit those exact assets only through `engine_sessiond` revision-safe operations.",
                "Preview candidates are transient and must be visibly labelled. Cooked data is derived, reproducible, and non-editable.",
                "Hidden editor databases, duplicated C++ grip constants, editable generated code, runtime-object mutation plus opaque serialization, and prefabs that copy profile fields are forbidden."
              ]
            },
            {
              "title": "Current Implemented Slice",
              "keywords": ["v1 compatible", "v2 skeleton", "v2 clip", "attachment profile", "typed handles", "native harness", "rest evaluation", "sampled evaluation", "revision"],
              "items": [
                "V1 skeleton/clip/attachment loading remains compatible while strict v2 skeleton/socket/clip-track/attachment parsing and validation now exist in `AnimationSystem`.",
                "Generation-tagged skeleton, bone, socket, and attachment handles invalidate after a successful reload; a failed reload retains the prior valid generation and snapshots.",
                "Isolated humanoid/rifle/pistol fixtures live under `animation/fixtures/spatial/`, outside authored and cooked roots.",
                "`npm run test:spatial-authoring-scaffold` compiles and executes the native parser harness; WSL `g++` is required on Windows.",
                "`shader_forge_spatial validate --animation-root <path>` reuses `AnimationSystem::loadFromDisk` and emits deterministic JSON counts plus skeleton/profile metadata with stable relative attachment source paths.",
                "`shader_forge_spatial cook` validates through the same loader and atomically stages complete socket/profile tables with relative source paths and no query handles or absolute machine paths; `engine spatial cook` exposes that already-built lane.",
                "`shader_forge_spatial evaluate-rest --animation-root <path> --attachment <id> --content-root <path> --data-foundation <path>` composes deterministic rest frames, resolves independent prefab-bound render-procgeo visual evidence and authored collision geometry through `DataFoundation`, and emits exact typed joint-limit plus capsule/item clipping diagnostics; the strict CLI exposes the same already-built lane.",
                "`shader_forge_spatial evaluate-sample --animation-root <path> --attachment <id> --phase <phase> --normalized-time <value> --content-root <path> --data-foundation <path>` resolves an exact authored motion-envelope sample, applies primary attachment and schema-v2 IK, then evaluates the same independent visual evidence, joint limits, and capsule/item surface clearance on the final pose.",
                "`shader_forge_spatial capture-sample` depth-tests the exact sampled pose as posed bone prisms plus the authored procgeo item box into four deterministic close-camera RGB PNGs and an optional strict authored root or parented `player_camera` frame. It emits source, camera, bounds, and lighting metadata, refuses existing or unsafe output targets, and is a renderer primitive rather than immutable review publication.",
                "Rest evaluation is a schematic query only: `pose.sampled=false`. Its exact authored visual-box outline is render-procgeo evidence and never substitutes for collision. Authored v2 cone-twist limits and diagnostic capsules are parsed and cooked; optional prefab box collision is independent typed `DataFoundation` source truth. Native rest/sample evaluation reports rest-relative swing/signed twist and capsule-axis-to-oriented-box surface clearance without mutating, clamping, or retargeting the pose. `shader_forge_spatial` emits exact typed joint-limit and clipping aggregates plus stable per-bone/per-capsule records; sessiond and Assets recompute and validate their identities, geometry, derived values, and aggregate truth fail closed. Missing inputs produce exact unavailable diagnostics and tangency is `CLEAR`. Assets presents joint limits as diagnose-only `PASS`/`FAIL` and capsule/item clipping as `CLEAR`/`OVERLAP`, never as runtime collision or review evidence. Native frames become evidence only after sessiond binds leases/revisions and atomically publishes an immutable packet.",
                "Sampled evaluation is read-only and not review evidence. V2 two-hand profiles apply deterministic two-bone secondary-hand IK using the palm socket and item-space pole, preserve limb lengths, and report separate physical reach, contact, angular, joint-limit, and clipping truth. V1 two-hand profiles remain visibly `PRE-IK`; one-hand profiles report `sampled_attachment_schematic_only`. Sessiond, Assets, and the typed `spatial_attachment_read` MCP tool expose this exact authored sampled evidence; candidate evaluation remains rest-only.",
                "`GET /api/spatial/attachment/evaluate` and `GET /api/spatial/attachment/evaluate-sample` stage exact animation files, every authored content TOML, and the data-foundation manifest; reject symbolic paths; recheck the complete sorted revision manifest; and return transient evidence without a lease, operation, journal entry, or persisted report.",
                "`POST /api/operations/spatial-attachment/preview` validates and evaluates exact staged baseline/candidate bytes under their expected old/new IDs, returns those reports transiently, requires exact write-lease coverage, rechecks the lease after evaluation, and only then records a labelled generic file-write candidate with durable public spatial context. A new-file baseline is null and evaluations never enter the journal.",
                "`POST /api/operations/:id/validate` is lease-free for previewed/approved spatial operations. It accepts at most 64 exact non-duplicate phase/time samples, verifies private proposed bytes in the complete staged source snapshot, native-validates and evaluates sequentially, rejects manifest/CAS drift, and persists only bounded controlled counts or safe candidate/sample failure identity.",
                "`POST /api/operations/:id/review-reservations` allocates an operation/session/agent-bound review ID so the caller can acquire the exact `spatial/review/<id>` write lease. `POST /api/operations/:id/recapture` then requires completed validation plus source-read, exclusive capture-write, and review-write leases; expands every authored time in selected phases; enforces `player_camera` for first-person/both; validates native metadata and PNG dimensions; rechecks revisions/leases; atomically publishes; and releases all leases.",
                "`GET /api/spatial/reviews/:reviewId` returns the immutable manifest. The capture route serves only PNG paths referenced by that packet, rejects symbolic/escaping paths, bounds file size, and emits immutable cache headers.",
                "The Assets workbench binds rest/sample evaluation to exact animation, content, and foundation revisions. Draft changes never move cached geometry; they mark it stale, and conflict remains visibly stale even when numeric values match.",
                "Front X/Y, Side Z/Y, and Top X/Z projections show native evaluator frames, the exact render-procgeo visual-box outline, the independent authored collision OBB, and diagnostic capsule segments. A bounded deep validator recomposes both boxes and clipping-derived truth and rejects unknown reasons, malformed geometry, unsafe manifests, oversized payloads, non-finite transforms, and unsafe projection bounds.",
                "Scene and spatial apply/undo share one semantic mutation dispatcher. The spatial branch independently native-validates and rest-evaluates staged current/resulting truth inside the existing file-mutation lane, rechecks the manifest without recursive locking, and rechecks the covering write lease before authored replacement. Validation failure writes nothing; native spatial child commands have a fixed timeout.",
                "`engine spatial preview|validate-operation|approve|reject|apply|undo`, `review reserve|read`, and `recapture` are strict adapters over that same sessiond contract. Validation consumes a strict BOM-free UTF-8 top-level sample array and needs no credential; review read is lease-free; preview/apply/undo/reserve/recapture use only the environment-provided coordinator credential where required and never acquire coordination or bypass sessiond.",
                "`npm run test:spatial-operations` covers revision-safe no-lease rest/sample GETs, path/symlink and exact-byte staging, strict operation-validation samples, controlled summary failures, complete source-manifest drift and stale-CAS rejection, sampled one-hand/v1-pre-IK/v2-applied-IK truth, native apply/undo revalidation, exact recapture leases, authored-camera gates, atomic immutable publication, binary capture reads, journal privacy, CORS, and temp cleanup.",
                "`npm run test:spatial-cli` drives the real CLI against deterministic sessiond and recording seams and validates preview/validation/review reserve/read/recapture/apply/undo routing, strict arguments, fatal UTF-8 input, server diagnostics, and credential redaction.",
                "The Assets workspace lists attachment profiles read-only. Lease-free native evaluation is still attempted when a readable source cannot be rewritten by the constrained parser. Explicit `Begin tuning` requests the exact profile lease and refreshes source after grant; only primary-grip translation and degree-displayed rotation are editable through strict byte-preserving source replacement.",
                "Candidates are labelled `NOT APPLIED`; Preview, Approve, Apply, Reject, and Undo use the same sessiond operations. `App` retains one sessiond SSE subscription and Assets uses its event epoch to refetch the active operation with session/selection/id guards. Approval updates actions. Conflict rereads source, preserves stale candidate evidence, and retains its captured connection only after parse/resource checks. Reject/apply/undo clear and reread evidence plus clean up only the connection captured by that event/action, and Undo after apply reacquires a fresh covering lease.",
                "Every rest or sampled Assets schematic is explicitly not review evidence. The authored visual box is neither a rendered mesh nor collision truth. Joint limits use diagnose-only aggregate/per-bone `PASS`/`FAIL`; capsule/item surface clearance uses aggregate/per-capsule `CLEAR`/`OVERLAP`, with exact unavailable states for both. Standalone native close/player-camera frames are also not packets; sessiond alone publishes revision- and lease-bound immutable evidence. Assets displays those frames as review evidence only after loading their immutable packet in a separate panel. V1 sampled two-hand evidence remains pre-IK while v2 exposes applied-IK numeric truth.",
                "Assets can validate and publish every exact authored time in the selected motion-envelope phase for a previewed or approved candidate, or load an existing review ID in the active session. Capture releases the tuning write lock, uses a fresh in-memory agent for exact source-read/capture-write/review-write leases, and requires `Begin tuning` again before mutation. The bounded packet parser rejects malformed schemas, attachment mismatches, duplicate samples, missing close cameras, unknown cameras, and unsafe PNG paths. The panel shows review, operation, sample, and camera identity and renders only packet-referenced PNGs.",
                "`npm run test:spatial-shell` validates exact source rewriting, independent authored-envelope parsing, sorted source-manifest guards, complete v1/v2 rest/sampled evaluator-schema/size/projection guards, exact phase/time/revision binding, one shared sessiond event subscription and authoritative operation refetch, Assets-only mounting, explicit locking, operation-only routing, immutable review-packet parsing and recapture/presentation wiring, accessible/responsive markers, v1 pre-IK/v2 applied-IK truth, clipping overlays, resolved-v2 pole projection, and credential-free evaluation GETs.",
                "`sf-mcp` exposes read-only `spatial_attachment_read`, lease-free `spatial_attachment_validate`, explicit `spatial_review_reserve` / `spatial_review_recapture` / `spatial_review_read`, and the typed review resource; the CLI exposes the same validation and review workflow. Adapters use selected operation/session authority and explicit caller-owned leases while exposing neither credentials, proposed bytes, nor raw native stderr. Assets uses the same sessiond packet workflow."
              ]
            },
            {
              "title": "Assets And Review Packets",
              "keywords": ["skeleton.toml", "attachment.toml", "reviewId", "cameras", "two-hand"],
              "items": [
                "The implemented loader accepts compatible v1 and strict v2 skeleton/socket/attachment-profile assets; current v2 spatial assets remain isolated fixtures rather than normal runtime or cooked content.",
                "Humans and agents share one `reviewId` and an immutable review packet with explicit sample cameras, resolution and framing, lighting, authored phase samples, source revisions, evaluated transforms and bounds, diagnostics, captures, operation id, and all lease ids. Sessiond reserves the ID, enforces exact source/capture/review leases, rechecks revisions, atomically publishes, and serves only referenced captures.",
                "Do not scrape a live cursor or camera to invent a packet.",
                "Two-hand weapons use dominant-hand item drive, then off-hand IK. VLM or visual scores never apply.",
                "Generated packets live under `build/spatial-reviews/<review-id>/`. Provider-specific `Saved/Codex` paths are forbidden.",
                "`sf-mcp` revision-bound `spatial_attachment_read`, lease-free `spatial_attachment_validate`, attachment mutation adapters, explicit review reserve/recapture/read tools, and the typed immutable review resource are implemented.",
                "Implementation is ordered after the existing operation layer and before broad World/Assets visual polish."
              ],
              "note": "See `docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md` for the full schema, rifle example, native handle example, harness gates, and staged implementation order."
            }
          ]
        }
      ]
    },
    {
      "id": "input-tooling-and-testing",
      "title": "Input, Tooling, And Testing",
      "description": "The engine has real named input plumbing, tooling-layout groundwork, and deterministic harness coverage that now needs to stay current as features widen.",
      "keywords": ["input", "tooling", "harness", "testing", "clean start"],
      "pages": [
        {
          "id": "input-and-tooling",
          "title": "Input And Native Tooling Foundations",
          "summary": "Gameplay, UI, and tooling controls are routed through named engine actions, while the native tooling layer has a panel registry and layout persistence groundwork.",
          "keywords": ["actions", "contexts", "tooling overlay", "layout"],
          "references": [
            "engine/runtime/include/shader_forge/runtime/input_system.hpp",
            "engine/runtime/src/input_system.cpp",
            "engine/runtime/include/shader_forge/runtime/tooling_ui.hpp",
            "engine/runtime/src/tooling_ui.cpp",
            "input/actions.toml",
            "input/contexts/gameplay.input.toml",
            "input/contexts/ui.input.toml",
            "tooling/layouts/default.tooling-layout.toml",
            "docs/specs/ENGINE-INPUT-SPEC.md",
            "docs/specs/ENGINE-TOOLING-UI-SPEC.md"
          ],
          "sections": [
            {
              "title": "Input System",
              "keywords": ["gamepad", "keyboard", "mouse", "named actions"],
              "items": [
                "`input/actions.toml` plus `input/contexts/*.input.toml` define the current action/context maps.",
                "Keyboard, mouse, and gamepad input are routed through engine-owned named actions and axes.",
                "The runtime currently consumes actions such as `runtime_exit`, `reload_runtime_content`, `save_runtime_state`, `load_runtime_state`, `toggle_physics_debug`, `select_previous_save_slot`, `select_next_save_slot`, `move_x`, `move_y`, `look_x`, `look_y`, `ui_accept`, and `ui_back`, with `F7` active for authored-content reload plus `F8`, `F9`, `F10`, `F11`, and `F12` active for quick-save, quick-load, physics-debug toggling, and runtime save-slot cycling."
              ]
            },
            {
              "title": "Tooling UI",
              "keywords": ["panels", "registry", "layout persistence", "imgui"],
              "items": [
                "The native tooling substrate currently has a named panel registry.",
                "Tooling layouts are loaded from text and session layouts can be saved back to disk.",
                "The current panel set covers runtime stats, input debug, log view, and debug state.",
                "The overlay summary now also carries first live gameplay-state context for the controlled entity, animation state, movement blocking, active save slot, interaction target, and physics-debug state during manual testing.",
                "Tooling overlay and panel toggles are already bound through the engine-owned input actions."
              ],
              "note": "Dear ImGui docking and real in-process native panel rendering are still ahead. The current slice is substrate, not the final tooling frontend."
            }
          ]
        },
        {
          "id": "testing-and-clean-start",
          "title": "Deterministic Harnesses And Clean Start",
          "summary": "The repo uses deterministic harnesses as the default regression lane, and the clean-start scripts run the current baseline before starting the active workspace.",
          "keywords": ["tests", "smoke", "build", "verification", "scripts"],
          "references": [
            "docs/guides/ENGINE-HARNESS-TESTING.md",
            "scripts/test-engine-shell-smoke.mjs",
            "scripts/test-engine-shell-layout.mjs",
            "scripts/test-engine-code-shell.mjs",
            "scripts/test-engine-viewer-bridge.mjs",
            "scripts/test-engine-packaging-scaffold.mjs",
            "scripts/test-engine-profiling-scaffold.mjs",
            "scripts/test-engine-scene-authoring.mjs",
            "scripts/test-engine-scene-runtime-scaffold.mjs",
            "scripts/test-engine-runtime-scaffold.mjs",
            "scripts/test-engine-save-system-scaffold.mjs",
            "scripts/test-engine-asset-pipeline.mjs",
            "scripts/test-engine-migration-fixtures.mjs",
            "scripts/test-engine-audio-scaffold.mjs",
            "scripts/test-engine-animation-scaffold.mjs",
            "scripts/test-engine-spatial-authoring-scaffold.mjs",
            "scripts/test-engine-spatial-tool.mjs",
            "scripts/test-engine-physics-scaffold.mjs",
            "scripts/test-engine-input-scaffold.mjs",
            "scripts/test-engine-tooling-ui-scaffold.mjs",
            "scripts/test-engine-data-foundation-scaffold.mjs",
            "scripts/start-dev-clean.sh",
            "scripts/start-dev-clean.ps1"
          ],
          "sections": [
            {
              "title": "Current Harnesses",
              "keywords": ["npm test", "sessiond", "viewer bridge", "runtime scaffold", "animation scaffold", "physics scaffold"],
              "items": [
                "`npm test` runs the shell smoke harness, including the integrated shell layout and native Code workspace contracts.",
                "`npm run test:shell-layout` validates strict v1 layout-only serialization, canonical fallback/reset, per-workspace independence, responsive side-pane geometry, narrow right-drawer reachability, and bounded short-viewport bottom-dock behavior.",
                "`npm run test:code-shell` validates revision-bound Code tabs, detached dirty-draft retention, current-file search, typed conflict matching, async session/tab/operation authority, and compare-before-refresh draft preservation.",
                "`npm run test:sessiond` validates local backend session/file flows, revision-safe file-write operations, bounded selected-operation exact diffs and summary-only degradation, durable bounded spatial-validation summaries/events with restart provenance and timestamp checks, journal recovery, writer serialization, loopback-only bind, immutable session roots, journaled code-trust effects, undo-vs-promote mutation-lane barriers, persisted `rootIdentity` migration, Origin/actor/code-trust gates, plus multi-agent leases, queue promotion, expiry, credential ownership, workspace cleanup, and isolation.",
                "`npm run test:viewer-bridge` validates build/runtime bridge events.",
                "`npm run test:packaging-scaffold` validates export-preset inspection, visible prep state, auto-bake packaging, release-layout generation, and extractable default ZIP contents through the CLI and `engine_sessiond`.",
                "`npm run test:profiling-scaffold` validates live diagnostics snapshots, persisted capture reports, and capture-history listing through the CLI and `engine_sessiond`.",
                "`npm run test:code-trust-scaffold` validates the shared code-trust policy summary, artifact hashes plus verification state, promote/quarantine transitions, approval queue listing/decision flows, allowed authored-content hot reload, approved deferred apply/compile operations, and rejected assistant-triggered engine load paths.",
                "`npm run test:scene-authoring` validates the shell scene-authoring surface plus deterministic scene/prefab file writes inside a session root.",
                "`npm run test:scene-runtime-scaffold` validates the first Phase 6 composed-scene and controlled-entity runtime slice.",
                "`npm run test:spatial-authoring-scaffold` compiles and executes the native v1/v2 skeleton/clip/socket/attachment harness, including deterministic clip sampling, exact envelope sampling, sampled attachment composition, layer truth, rejection, reload, and typed-handle behavior; WSL `g++` is required on Windows.",
                "`npm run test:spatial-tool` requires a native compiler and runs the production validate/cook/evaluate-rest/evaluate-sample tool against isolated fixtures, checking deterministic rest/sample geometry, unchanged rest shape, v1 pre-IK/one-hand truth labels, v2 two-bone IK and tolerance truth, strict native/`engine` CLI behavior, and failure cases; WSL `g++` is required on Windows and `g++` elsewhere.",
                "`npm run test:spatial-cli` drives the real CLI against an injected-validator sessiond and validates preview/review/apply/undo, strict arguments, fatal UTF-8 input, server diagnostics, and credential redaction.",
                "`npm run test:runtime-scaffold`, `test:save-system-scaffold`, `test:data-foundation-scaffold`, `test:asset-pipeline`, `test:migration-fixtures`, `test:audio-scaffold`, `test:animation-scaffold`, `test:physics-scaffold`, `test:input-scaffold`, and `test:tooling-ui-scaffold` validate the native bring-up and first cook slices."
              ]
            },
            {
              "title": "Clean Start",
              "keywords": ["wsl", "powershell", "baseline"],
              "items": [
                "`./scripts/start-dev-clean.sh` is the Unix/WSL clean-start path.",
                "`powershell.exe -ExecutionPolicy Bypass -File .\\scripts\\start-dev-clean.ps1` is the Windows clean-start path.",
                "Both scripts remove generated outputs, rerun the current deterministic baseline, start `engine_sessiond`, and then launch the active shell workflow."
              ]
            }
          ]
        },
        {
          "id": "current-boundaries",
          "title": "Current Boundaries And Next Widening Passes",
          "summary": "The project is moving through dependency-first slices; this guide should say clearly what exists today and what still needs widening.",
          "keywords": ["boundaries", "limitations", "next steps", "widening"],
          "references": [
            "plans/ENGINE-IMPLEMENTATION-PLAN.md",
            "docs/specs/ENGINE-SPATIAL-AUTHORING-SPEC.md",
            "docs/reference/ENGINE-REFERENCE-GUIDE.md",
            "docs/reference/ENGINE-REFERENCE-GUIDE.json"
          ],
          "sections": [
            {
              "title": "What Exists Now",
              "keywords": ["current state", "implemented"],
              "items": [
                "A React/Vite shell workspace with persistent backend-owned sessions, file preview, source control, terminal tabs, runtime control, and strict per-workspace browser-persisted layouts with accessible resizing/reset plus a narrow right drawer.",
                "A real shell-side scene authoring workflow with repo-backed `.scene.toml` and `.prefab.toml` save/reload/duplicate flows plus placed-entity hierarchy, transform editing, first prefab component payload editing, and active-session-root runtime handoff.",
                "A real native SDL3/Vulkan runtime slice with input, tooling, data-foundation, audio, animation, physics, first composed scene-runtime hooks, session-root launch alignment from the shell/session backend, and a first runtime save-system lane.",
                "Text-backed scene, prefab, data, effect, procedural-geometry, audio, animation, and physics roots represented in the repo.",
                "A first CLI bake lane that emits staged cooked outputs, generated-mesh preview artifacts, and staged cooked audio, animation, plus physics metadata.",
                "A first CLI and shell/sessiond packaging lane that resolves source-controlled Debug/Release desktop presets, emits release layouts under `build/package/`, records package reports plus launch manifests, and executes sibling ZIP hooks.",
                "A first CLI and shell/sessiond profiling lane that captures workspace diagnostics plus live runtime/build log context into JSON reports under `build/profiling/captures/`.",
                "A first CLI migration lane that detects supported source-engine project shapes, emits normalized migration manifests plus reports, converts the current fixtures into first-pass Shader Forge project skeletons, and preserves startup-scene intent through exact setting provenance or explicit fail-closed/approximated outcomes.",
                "A first code-trust lane with source-controlled policy data, shared sessiond/CLI evaluation, tracked assistant/code-path artifacts, explicit review queues for `review_required` operations, and assistant-triggered compile/load/apply gating.",
                "A hardened `engine_sessiond` revision-safe text-file operation workflow with preview, approval, journaled apply/undo, journaled code-trust effects, serialized CLI provenance transitions, immutable workspace identity, append-only recovery provenance, structured conflicts, loopback-only bind, local Origin filtering, restart-safe history, and the durable bounded validation-summary/event storage boundary for spatial operations. `sf-mcp` now consumes its lease-gated spatial attachment and whole scene/prefab families; other MCP operation families still require explicit resource and policy contracts. Cooperative engine clients are covered; hostile out-of-process filesystem swaps at the OS syscall boundary are not an adversarial security guarantee.",
                "A native spatial schema/query/cook/rest and sampled attachment evaluation slice with v2 two-bone secondary-hand IK, explicit v1 pre-IK compatibility, and diagnose-only joint-limit aggregate/per-bone PASS/FAIL, plus a constrained Assets tuner and accessible native-evaluated three-projection rest/sampled rig schematics with resolved v2 pole projection, exact full-input evidence binding, guarded event reconciliation, and no rendered-review claim in the shell.",
                "A searchable in-app guide and repo-native external-agent guide."
              ]
            },
            {
              "title": "What Still Needs Widening",
              "keywords": ["future work", "phase expansion"],
              "items": [
                "The shell still needs deeper UX and more app-native surfaces beyond the preserved code bridge.",
                "Scene authoring still needs transform gizmos, deeper scene/component payload authoring, and bake-back flows beyond the current text-backed entity plus prefab-component slice.",
                "The runtime still needs a full mesh/material rendering path, richer prefab/component instancing beyond the current projected debug proxies, broader scene simulation, and broader native verification.",
                "The save system still needs wider world-state persistence, multiple slots, profile/settings support, and migration-aware tooling beyond the current quick-save lane.",
                "Packaging still needs runtime-build orchestration, installers, signing, platform-specific presets, additional hooks, and real cooked-runtime loading beyond the current desktop Debug/Release layouts and ZIPs.",
                "The content pipeline still needs the real FlatBuffers writer, import lanes, and deeper preview surfaces beyond the first staged bake path.",
                "Audio still needs the real playback backend, bus mixing/control, and preview surfaces on top of the new authored event-definition lane.",
                "Animation still needs runtime graph consumption of the deterministic sampler, blending, graph-parameter control, root-motion application, and preview tooling on top of the authored graph-definition lane.",
                "Spatial authoring still needs generic bake/runtime consumption, runtime collision/physics integration, and gameplay attachment rendering beyond the implemented operation validation, authored close/player-camera capture, revision-bound spatial attachment read, lease-gated recapture, immutable packet reads, CLI/MCP/Assets review adapters, mutation revalidation, and diagnostics.",
                "Physics still needs the real backend integration, sweeps, joints, character support, and richer debug gizmos/capture on top of the new authored query-definition lane.",
                "Migration still needs high-fidelity scene/prefab graphs, real asset payload import, gameplay conversion, conflict-safe reimport, and exporter-assisted Unreal extraction beyond the current fixture-backed project skeleton and startup-setting lane.",
                "Code trust still needs stronger artifact verification, trust-promotion workflows, and real code hot-reload contracts beyond the current policy-and-approval slice.",
                "Activity still needs safe explicit coordination before it can apply or undo. Further MCP mutation likewise needs engine-owned resource keys and lease enforcement; entity/component patching and general asset operations are not in this slice.",
                "Profiling still needs Tracy/RenderDoc integration, GPU and memory diagnostics, native profiling panels, and deeper performance-regression workflows beyond the current diagnostics snapshot lane.",
                "Tooling UI still needs the full Dear ImGui frontend and deeper authoring/profiling panels."
              ]
            }
          ]
        }
      ]
    }
  ]
}
